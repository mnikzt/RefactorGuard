
●项目描述： 
面向 Java Maven 项目，结合静态分析、LLM 语义判断与多 Agent 修改验证回滚闭环，实现七种代码坏味道检测与安全重构。 
●技术栈： Python 3.11+、Java 17、Maven、JavaParser、Symbol Solver、LLM Agent、ReAct、OpenAI-Compatible API、httpx、Pydantic、pytest、Ruff、Mypy、uv、Git、JaCoCo、PMD CPD
●主要工作： 
1. 代码坏味道识别：针对 LLM 全仓搜索成本高的问题，先由 JavaParser AST、Symbol Solver 和 PMD CPD 静态分析仓库代码生成问题候选，再由 LLM 结合源码完成最终筛选，相比 LLM 直接搜索平均耗时降低 56.5%，平均 Token 消耗降低 67.5%。 

2. 针对单 Agent 自我验证偏差：拆分测试、修改和验证 Agent，并隔离提示词与对话记录；修改前,测试 agent 通过 JaCoCo检查目标代码测试覆盖，并生成行为锁定测试；验证失败则反馈修改 Agent 重试，重构成功率由 Pass@1 的 75.0%提升至 88.3% 

3. 针对 LLM 容易越界修改的问题：限制其只输出结构化行级编辑，补丁应用前校验文件白名单、路径边界和行号范围，应用后通过 JavaParser 检查语法结构及公开 API 变化，校验失败则自动恢复原文件。 

4. 验证与回滚：验证 Agent 结合重构计划与真实 Diff，判断目标坏味道是否消除且未引入新问题，并执行 Maven 编译测试和工作区哈希检查；验证失败时将问题反馈给修改 Agent 循环修复，多轮重试仍未通过则自动恢复全部文件。 

5. 针对跨会话知识无法复用的问题：设计 PAI.md 项目记忆、全局长期记忆和会话历史三层记忆，按任务检索相关信息注入上下文，并自动压缩早期对话，保留用户目标、关键结论和待办。 

6. 工具并行调用：多个只读工具通过 AsyncIO 并行执行，写操作保持串行；工具阶段平均耗时降低 57.6%。 


## 1. 亮点 1 口语逐字稿（Q1–Q22）

怎么用：每题就是你开口要说的话，按 40～70 秒练。括号里是可选补充，被追问再加。

---

### 基础问 1：什么是静态分析？

静态分析就是不运行程序，而是直接看源码结构、语法树、符号关系和规则，去发现潜在问题。  
在我的项目里，静态分析主要承担“先把搜索空间缩小”的职责。  
它先用 JavaParser、Symbol Solver 和 CPD 找出坏味道候选，再让 LLM 结合局部源码判断这个候选到底值不值得改。  
和动态分析相对，动态分析是把程序跑起来，通过测试、日志、运行时行为来判断问题。

---

### 基础问 2：这 7 种坏味道是怎么选的？你怎么理解坏味道？

我理解的代码坏味道不是 bug，而是会降低可读性、可维护性和可扩展性的结构信号。  
它不一定马上导致程序错误，但会让后续修改更难、更容易出错。  
比如方法太长、类太大、重复代码、条件逻辑复杂、命名不清、死代码，或者一个方法总是在操作别的类的数据，这些都说明代码结构可能不健康。

我选这 7 类，一方面是因为它们比较经典，Fowler 的 Code Smells、Sonar、PMD 这类工具里也常见；另一方面是因为它们适合自动化处理。  
像长方法、大类、复杂条件、重复代码，可以先用静态分析生成候选；像命名、特性依恋、死代码，则需要 LLM 再结合语义做筛选。  
所以这些坏味道既有面试官熟悉的理论基础，也符合我这个“静态候选 + LLM triage + 安全重构”的工程闭环。

这 7 类分别是：Long Method、Large Class、Duplicate Code、Feature Envy、Unclear Naming、Complex Condition 和 Dead Code。  
它们对应的重构方向大概是提取方法、拆类、提取公共逻辑、移动方法、重命名、提取解释性变量或删除无用代码。

---

### 基础问 3：JavaParser AST、Symbol Solver 和 PMD CPD 分别是什么？

JavaParser AST 负责看清“代码长什么结构”。  
它能把 Java 源码解析成抽象语法树，拿到类、方法、行号、分支数、嵌套深度这些结构信息。  
所以长方法、大类、复杂条件这类规则主要靠它。

Symbol Solver 负责看清“这个名字到底指向谁”。  
AST 只能告诉我这里调用了一个方法、访问了一个字段，但不一定知道它属于哪个类。  
Symbol Solver 可以解析调用和字段的归属类型，所以 Feature Envy 和 Dead Code 这类规则会更依赖它。

PMD CPD 是复制粘贴检测工具。  
它会找重复代码片段，并告诉我重复块出现在哪些文件、起始行号、重复行数或 token 数。  
所以重复代码这块我没有自己造轮子，而是直接复用 CPD。

整体上，JavaParser 负责结构，Symbol Solver 负责符号归属，PMD CPD 负责重复代码。  
它们先生成低成本候选，LLM 后面只需要判断这些候选，而不是全仓盲搜。

---

### 基础问 4：56.5% 耗时降低、67.5% Token 降低是怎么测出来的？

这是我做的项目内 benchmark。  
我选了 4 个 Java Maven 项目，一共 20 个坏味道识别任务。  
Baseline 是让 LLM 通过只读工具直接全仓搜索；我的方案是先用 AST、Symbol Solver 和 CPD 生成候选，再让 LLM 对候选做语义筛选。

统计口径有两个：一个是从任务开始到输出结构化结果的 wall-clock time，另一个是 LLM API 返回的 input + output Token。  
最终平均耗时从 312 秒降到 136 秒，Token 从 5.82 万降到 1.89 万。  
换算下来就是耗时降低大约 56.5%，Token 降低大约 67.5%。

样例一部分来自开源 Java Maven 项目的真实代码，一部分是 AI 辅助生成的坏味道变体。  
但生成后会人工复核标签，并且保证样例能编译、能运行，避免直接把 AI 生成内容当作真实标注。  
我对外会说清楚：这是项目评测集上的结果，不是线上大盘数据。

---

### Q1：LLM 全仓搜索贵在哪？为啥不用 RAG？

其实贵主要贵在三块。  
第一，Agent 会不停 list、search、read，工具轮次一多，时间就被拖长了。  
第二，每次把大段源码塞进上下文，Token 涨得很快。  
第三，同一仓库扫几遍，覆盖集合还不一定一样，召回不稳定。

为啥不用 embedding RAG 呢？其实一开始我的设想是用RAG的，但实际上因为坏味道很多是结构信号，比如方法多长、嵌套多深、调用到底属于谁、有没有复制粘贴。这类东西用 AST、规则、CPD 更稳，也好解释。  
RAG 更适合“按一句话找相关代码”，不太适合保证“长方法、重复块一定被系统性捞出来”。  
所以我的分工是：确定性搜索交给静态扫描，值不值得改交给 LLM。triage 的时候它还可以局部 search，但不再负责全仓发现。

---

### Q2：三件套怎么分工？只能留一个留谁？

简单说就是各管一块。  
JavaParser 看结构：方法从哪行到哪行、分支多少、嵌套多深、类里有多少成员。长方法、大类、复杂条件主要靠它。  
Symbol Solver 看归属：这个调用、这个字段到底是哪个类的。特性依恋强依赖它，死代码也优先用它。  
PMD CPD 专门找复制粘贴重复，重复代码这条链路基本就交给 `mvn pmd:cpd-check`。

如果只能留一个，我会留 JavaParser。因为没有靠谱 AST，行号和结构指标都不准，后面 triage 和补丁校验都会垮。没有 Symbol，特性依恋会变弱；没有 CPD，重复代码会缺一块；但主流程至少还能跑。

---

### Q3：你们偏召回还是偏精确？阈值松紧怎么影响？

两阶段目标不一样。静态分析我偏召回，先尽量把可疑点捞出来；LLM triage 偏精确，必须看到源码证据才 accept。  
原因很简单：漏报后面很难补，我们不会再让 LLM 全仓盲搜；误报可以在第二阶段砍掉。

阈值调松，候选就多，Token 和耗时会上去，但更不容易漏；调紧成本低，但命名、特性依恋这类更容易漏。我倾向规则稍微松一点，LLM 收得严一点，因为后面还有计划、覆盖率、编译测试和回滚兜底。  
结构类阈值比较固定，比如长方法大概八十行以上，或者分支、嵌套超标；特性依恋本身就偏紧；命名和死代码更靠 LLM 把关。

（别说“我们又要高召回又要高精确”，直接说两阶段目标不同。）

---

### Q4：哪些靠 AST？哪些必须 Symbol？举个误判例子

长方法、大类、复杂条件，基本看 AST 结构就行。命名不清用黑名单启发式，不一定要 Symbol。重复代码走 CPD。  
真正必须 Symbol 的是特性依恋：解析不成功我直接不报。死代码优先用 Symbol 的调用关系，解析失败才退回“这个名字全项目出现几次”。

误判很好举。比如方法里一堆 `customer.getX()`，AST 只看见调用名是 getX，不知道它是不是 Customer 上的。要是只按名字统计，本类同名方法、别的无关 getX 都可能被算成外部依赖，特性依恋假阳性会很高。所以代码里必须拿到归属类型才继续判。

---

### Q5：Symbol Solver 挂了怎么办？

不是一刀切。  
特性依恋：解析失败就跳过，宁可不报，也不要乱报。  
死代码：有解析签名就按调用图看；没有就退化成标识符出现次数，并且在 evidence 里标清楚用的是 fallback。  
另外说实话，我们 Type Solver 主要配了反射和项目源码根，没有把所有 Maven 依赖 jar 都喂进去，所以第三方类型有时解析不好，这是已知边界。   
如果连 AST 整体都挂了，scan 直接失败，我不会偷偷改成正则扫描糊弄过去。
JavaParser AST
会挂：helper 起不来、mvn 失败、返回非 0、JSON 坏了、某个文件没解析出 AST。
处理：直接抛 JavaAstError，run_scan 里 catch 后变成「AST 解析失败，scan 已停止」。不会退回正则瞎扫——结构信号错了，后面候选和补丁校验都不可信。

PMD CPD
会挂：没装 plugin（交互里先问安装，不装就取消 scan）、mvn pmd:cpd-check 跑不起来、非 0 又解析不出 duplication。
处理：抛 CpdError，同样停扫。returncode==0 当「没重复」；非 0 但能解析出重复块，才正常产 issue。

和 Symbol 的对比（面试一句话）
Symbol 是局部降级（特性依恋不报、死代码 fallback）；AST / CPD 是全局门禁——挂了就停，不强行出半套结果。
---

### Q6：为什么重复代码用 PMD CPD，而不是自己做 AST 子树同构 / token hash？CPD 的 minimum tokens 怎么定？定太低/太高分别会怎样？

自己做 AST 同构或者 token hash，要处理格式化差异、小改动变体，MVP 阶段不划算。CPD 是现成的 token 级重复检测，又和 Maven 集成方便：装好 pmd 插件，跑 cpd-check，把重复行数和出现位置解析出来就行。

minimum tokens 这块，我安装插件时没有在代码里强行改 CPD 默认阈值，基本走插件默认。我们自己用重复行数定严重级别，大概四十行以下中等，再高就偏高，并且把多个重复位置写进 evidence。  
阈值太低会刷屏，太高会漏短重复；要调的话改 pom 里的 CPD 配置，而不是改 Python 解析逻辑。当前优先保证“能稳定打出可定位的候选”。

---

### Q7：哪些规则就够用？去掉 LLM 会怎样？

结构比较客观的，比如长方法、大类、复杂条件、重复代码，规则本身可信度更高。  
强依赖 LLM 的主要是命名不清——黑名单很粗；还有死代码——反射、框架回调容易误报；特性依恋有时也是设计判断，不单是计数。

如果去掉 triage，命名和死代码噪音会明显起来，结构类也会把一些“确实很长但暂时不该动”的代码推给用户。内测体感上，没 triage 时人工要看的噪音大概能到两三倍这个量级，尤其是命名，所以第二阶段是必要的。这个数是量级观察，我不会说成精确 offline 指标。

---

### Q8：候选长什么样？为啥不只丢个路径？

候选是结构化对象，不只是路径。里面有编号、坏味道类型、严重度、文件、符号名、起止行、证据和指标、影响说明、建议怎么重构、风险等级。  
给 LLM 的时候，还会带上局部源码摘要、相关测试、直接调用方，它不够还可以再 read、再 search。

如果只丢路径，模型还得自己重新定位，Token 和漏判都会回来。我们省成本的关键，就是把问题收成“看这几十个带证据的候选”。  
另外只有 accept 会进最终 issues；reject 和 uncertain 留在审计里。默认最多仔细看 20 个，超出的直接标 uncertain，并写明没调查。

---

### Q9：LLM 的 accept / reject / uncertain 怎么用？uncertain 进不进重构计划？会不会成为逃避决策的出口？

accept 才能进后续计划和修改。  
reject 就是明确误报，不自动改。  
uncertain 是证据不够，或者超出 limit 还没来得及看——同样不进自动重构。

防偷懒这块，prompt 里要求它先看目标源码，必要时看调用方和测试；accept 必须带具体源码证据，测试里也卡了“没证据不能 accept”。所以 uncertain 是显式暂缓，审计里看得见，不是悄悄当成通过。

---

### Q10：候选很多时怎么控成本：截断、按 severity 排序，还是分批 triage？有没有「静态阶段本身比 LLM 还慢」的情况？

硬门槛是 triage 默认只深挖前 20 个，后面标 uncertain。  
排序上说实话，现在是按文件路径和行号稳定排完再编号，还没有先按严重度送进 LLM；accept 之后再用模型给的 priority 排一下展示顺序。这个点被问到我直接承认，后续可以改成严重度优先。

大仓库上，全量 JavaParser 加一次 Maven CPD，静态阶段确实可能占不少墙钟时间。但跟 LLM 全仓多轮搜索比，通常还是更便宜、也更可预期。现在用 limit 先把 LLM 成本箍住；下一步可以做严重度预排序、按模块扫、或者缓存 CPD 结果。

---

### Q11：扫描是增量还是每次全量？大仓（几千 Java 文件）的瓶颈在 JavaParser、Maven CPD，还是 LLM？


当前是全量。每次 scan 会把项目里的 java 文件收集一遍，跳过 target、.git 这些目录，然后做 AST，再跑一遍 CPD。没有文件级增量缓存。  
不过 triage 会复用这次扫出来的 AST 快照，避免精排阶段再解析一遍。

大仓瓶颈我优先看两块：JavaParser 全量解析，和 Maven CPD。LLM 因为有 20 个上限，反而不是最容易炸的那个。以后可以按 git diff 增量，或者按模块过滤。
     
---

### Q12：Baseline「LLM 直接搜索」具体长什么样？允许哪些工具、几轮、有没有超时/预算上限？预算不同会不会让对比不公平？

这是项目内 benchmark。我选了 4 个 Java Maven 项目，一共 20 个坏味道识别任务，模型和工具集保持一致。  
Baseline 是让 LLM 用只读工具自己全仓搜；我们是先静态出候选，再让 LLM 做语义筛选。  
比的是从任务开始到输出结构化结果的耗时，还有 API 的 input 加 output Token。大概耗时从 312 秒降到 136 秒，Token 从 5.82 万降到 1.89 万，对应那两个百分比。  
两边都没有故意卡死轮次去刷分。样本不算大，我汇报时会说清楚是评测集上的结果，不是线上大盘。

---

### Q13：会不会挑好做的样本？有没有分层看？

任务覆盖了七类坏味道，仓库规模也不完全一样；有真实开源代码，也有人工复核过的合成变体。  
20 个样本谈不上很严格的分层统计，所以我对外主要报总体均值。内部抽查过结构类和语义类：结构类两阶段优势更稳，命名类更吃 LLM。我不会把这组小数讲成大规模 A/B。

---

### Q14：耗时降 56.5%、Token 降 67.5%，质量有没有一起报？例如相对人工标注的 Precision / Recall / F1，或 triage 前后候选数变化？

简历上这两个数字主打效率。质量我看三层：accept 抽检是不是真问题；reject 有没有把误报砍掉；后面重构能不能过编译、测试、覆盖和验证 Agent。  
坏味道本身有主观性，尤其命名，所以我更强调“值不值得自动改”，而不是死磕一个大盘 F1。效率有完整对比；质量目前以抽检加下游成功率为主。     

---

### Q15：样例里有一部分 AI 生成坏味道——怎么避免「规则按生成方式写、指标虚高」？人工复核的标准是什么？

会有这个风险，所以我们生成之后要人工复核标签，并且用静态规则和单元测试保证样例能编译、能跑，不会把模型生成结果直接当金标准。  
评测比的是：同一批标签上，两种识别路径谁更省时间和 Token；不是看规则有没有刚好拟合生成器。合成样例主要用来补真实仓库里比较少的类型，比如特性依恋、复杂条件。

---

### Q16：Token 统计含不含静态分析、CPD、本地 Java helper？若只比 API Token，端到端成本叙事是否完整？

简历上的 Token，指的是 LLM API 的 input 和 output。  
静态分析、CPD、Java helper 是本地跑的，算进总耗时，但不计入 API Token。  
完整说法就是：端到端更省时间，计费 Token 降得更明显；我们是用本地 CPU 换更稳的召回。如果面试官问总拥有成本，我可以补本地耗时占比，但不会把本地成本和 API Token 混成一个数。

---

### Q17：为什么不上 SonarQube / SpotBugs 一整套，而要自建规则 + CPD？边界在哪？

因为我的目标不是再做一个质量平台，而是给自动重构提供可控的候选入口。  
自建这几条规则，产出直接对上我们的 issue 模型、计划和补丁白名单；重复代码这块就复用 CPD，它最擅长。  
Sonar 做门禁很强，但规则集大、接入重，还要跟后面的 LLM triage、多 Agent 修改对齐，成本更高。我的边界是：质量门禁可以继续用 Sonar；自动重构流水线用小而可控的规则集加 LLM。

---

### Q18：静态规则漏报（例如跨文件语义重复、命名问题）怎么补？LLM 还有二次全仓搜索吗，还是接受漏报？

不会再开一轮全仓坏味道发现，不然前面省下来的成本和稳定性就没了。  
triage 时 LLM 可以对当前这个候选去读文件、搜一下引用，但那是补充证据，不是重新当扫描器。  
漏了就调阈值、补符号信息、补 CPD，或者让用户指定文件和 issue。靠 LLM 盲搜补召回，我觉得方向不对。

---

### Q19：漏报你能接受吗？

有限漏报可以接受，高置信误报带着自动改，我不能接受。  
因为我们后面是真改代码的，错 accept 一次，成本是改文件、跑测试，还可能回滚；漏一个不太清晰的命名，代价小得多。  
所以在这个场景下，我优先要精确率和可验证性，而不是追求完美召回。

---

### Q20：如果支持 Python 或 Go，什么能复用？

流水线思路可以复用：先出候选，再 triage，再计划、打补丁、验证。决策协议、只读工具箱形态、审计落盘、多 Agent 隔离，这些也能留。  
必须重做的是语言前端：AST 和符号解析、重复检测工具、还有构建测试覆盖这一套。  
我会把边界画在“语言前端产出统一候选”，和“后面跟语言无关的编排层”之间，这样扩语言时不会把整条链路推翻。

---

### Q21：出现「CPD 报重复，LLM 也 accept，但重构后行为变了」——问题更可能出在检测、triage，还是后面的修改/验证？检测阶段还能加什么约束？

检测和 triage 只说明“这里像重复，值得抽”；行为被改坏，更常见是修改阶段抽公共逻辑抽错了，或者测试没锁住，或者验证没拦住。  
检测侧能加强的是：证据里带齐多个重复位置；triage 必须对比几处副本有没有微妙差异，不能只看见 CPD 报了就 accept。  
下游一定还要有行为锁定测试、Maven 测试，以及验证 Agent 看真实 diff。如果两处代码本来就不完全一样，却被当成可合并，那 triage 过度信任 CPD 也有责任。

---

### Q22：这不就是规则加 LLM 吗？难点在哪？

表面看确实是两阶段，但难点不在“会不会调模型”，而在怎么把不确定的 LLM 关进可重复的工程闭环里。  
第一，证据链要完整：静态指标进 evidence，accept 必须引用具体源码行。  
第二，工具失败要可见，该降级降级，不装成功。  
第三，accept、reject、uncertain 有明确协议，还落审计，跟后面的计划、补丁、验证对齐。  
第四，让 LLM 只做局部语义判断，不做全仓搜索器。

跟 Sonar 的差别也在这：我们不是堆更多 lint 规则，而是为“能安全自动重构”设计入口契约。

---

### Q23：这些规则阈值是怎么定的？有什么考量？

这些阈值不是模型训练出来的，也不是论文里的最优解，本质上是工程启发式。  
我的思路是：静态阶段服务召回，不直接做最终判决，所以阈值不能太保守。比如长方法看 80 行、分支 12、嵌套 4；大类看 500 行、方法数和字段数；复杂条件看嵌套和 `&&` / `||` 个数。这些量级和常见代码规范、Sonar/Checkstyle 一类工具的直觉接近，面试时也比较好解释。

我没有只看单一行数，而是尽量用多信号。比如一个方法可能只有六七十行，但分支特别多、嵌套特别深，它同样难维护；反过来，一个方法行数稍微长一点，但结构很平，也不一定要马上重构。  
所以规则只是先把可疑点捞出来，后面还要让 LLM 结合源码判断。

严重度也分两档：刚过线一般是 MEDIUM，明显翻倍才算 HIGH，比如长方法超过 160 行，或者大类超过 1000 行。这样后续排序和人审不会全都挤在一个等级里。

语义类我会更谨慎。特性依恋门槛偏紧，要外部成员访问明显多于本类，而且集中在一个外部类型上，避免误报。命名和死代码本来就容易误判，所以风险等级偏低，更多交给 LLM 收口。

如果被追问有没有大规模标定，我会如实说：目前没有做跨项目网格搜索，阈值是按工程经验和样例集校准的。后续真正要产品化，会看 `candidates.json` 里的误报、漏报分布，再按项目类型调参，而不是假装有一个全局最优阈值。

一句话说就是：这些阈值是“静态偏召回、LLM 偏精确”这个设计下的启发式，不是真理；它们负责生成候选，最终判断交给证据和 triage。

---

### Q24：现在一般都靠 Agent Coding 了，Agent 写代码还会有坏味道吗？

会有，而且我觉得 Agent Coding 反而会让坏味道检测更重要。  
Agent 解决的是“更快生成代码”，不是天然保证设计质量。它经常能写出能编译、能过测试的代码，但为了完成当前任务，容易做局部最优。

比如它会参考相似文件照着写，所以重复代码会更多；多轮修 bug 时，它可能不断加 guard、fallback、特殊分支，方法能跑，但条件越来越复杂；它也可能把逻辑塞进当前最好改的类里，导致大类或者特性依恋。命名也常见 `process`、`handle`、`manager`、`helper` 这种泛化词。

测试通过也不等于没有坏味道。测试主要说明行为没坏，但坏味道影响的是下一次修改成本，比如以后要改业务规则时，重复代码要改多处，复杂条件没人敢碰，大类影响面很大。

所以我这个项目不是和 Agent Coding 冲突，而是补它的短板。Agent 负责快速生成和修改代码；静态分析 + LLM triage 负责发现这些代码里可能累积的结构问题；后面的测试、验证和回滚负责保证重构安全。  
AI 写代码越多，越需要这种自动化的质量扫描和安全重构闭环。

---

### 开场 30 秒（先背这个）

我没让 LLM 直接全仓搜坏味道，而是先用 JavaParser、Symbol Solver 和 PMD CPD 做静态扫描，生成结构化候选，再让 LLM 结合局部源码做最终筛选。  
静态偏召回，LLM 偏精确；解析失败按规则降级或跳过；只有 accept 才会进入后面的重构。  
在我那个评测集上，平均耗时大概降了 56.5%，Token 降了 67.5%。核心不是又接了个模型，而是把搜索交给确定性工具，把判断关进带证据的流水线里。

## 2. 亮点 2 口语逐字稿（多 Agent 自我验证偏差：Q1-Q40）

### Q1：为什么单 Agent 会有“自我验证偏差”？你遇到的具体表现是什么？

我的理解是，单 Agent 同时负责写代码和验证代码时，很容易出现确认偏差。  
它前面刚写完一个方案，后面再验证时，会更倾向于证明自己的方案是对的，而不是主动找问题。  
具体表现就是：它会忽略 diff 里一些越界修改，或者测试通过了就认为重构成功，但其实目标坏味道可能没有真正消除。  
所以我把写代码和验代码拆开，避免一个 Agent 既当运动员又当裁判。

### Q2：为什么拆成测试 Agent、修改 Agent、验证 Agent，而不是一个 Agent 里用不同 prompt 分阶段执行？

一个 Agent 分阶段 prompt 也能跑，但它的对话历史和前面判断还是连续的。  
它已经知道自己为什么这么改，后面验证时很容易被自己的解释带偏。  
我拆成三个 Agent，是为了让它们的目标、提示词、工具权限和上下文都不同。  
测试 Agent 只负责锁行为，修改 Agent 只负责最小编辑，验证 Agent 只负责挑错和拒绝。

### Q3：三个 Agent 的职责边界怎么划？测试 Agent 能不能看修改计划？验证 Agent 能不能看修改 Agent 的思考过程？

测试 Agent 可以看重构计划和目标 issue，因为它要知道该锁哪段行为，但它不能改生产代码。  
修改 Agent 可以看计划、issue、允许修改文件和上一轮验证反馈，但只能通过结构化编辑改白名单里的文件。  
验证 Agent 主要看计划、真实 diff、命令结果、coverage 和源码证据，不看修改 Agent 的完整思考过程。  
我希望验证 Agent 基于事实证据判断，而不是被修改 Agent 的自我解释影响。

### Q4：你说隔离提示词和对话记录，具体隔离了什么？如果共享上下文，会带来什么风险？

隔离的是角色目标、system prompt、可用工具和对话历史。  
测试 Agent 的 prompt 强调行为锁定，修改 Agent 强调按计划做最小改动，验证 Agent 强调独立审查真实工作区。  
如果共享上下文，验证阶段可能会继承修改阶段的理由，变成“帮自己找通过理由”。  
所以我只把必要的结构化结果传给下一阶段，比如 plan、coverage、diff、verification feedback。

### Q5：多 Agent 之后会不会引入更多不确定性？怎么保证协作链路可控？

会，多 Agent 一定会让链路更复杂。  
所以我没有让它们自由聊天，而是用结构化对象和本地工具把边界卡住。  
比如测试写入要走测试文件 allowlist，修改要走 `apply_edits`，验证必须跑 Maven、JaCoCo、inspect diff。  
也就是说，Agent 负责判断，但关键状态以工具结果和真实工作区为准。

### Q6：修改前为什么要先检查 JaCoCo 覆盖率？覆盖率不足为什么不能直接让修改 Agent 开始改？

因为重构的核心前提是行为不能变。  
如果目标代码没有覆盖，修改后测试通过也不能说明行为没坏，只能说明现有测试没测到。  
所以修改前要先跑 JaCoCo，看目标区域有没有覆盖。  
覆盖不足时先补行为锁定测试，再让修改 Agent 开始改，这样风险更可控。

### Q7：你检查的是类覆盖、方法覆盖，还是目标行覆盖？为什么？

代码里更接近目标行覆盖。  
`CoverageAnalyzer` 会从 JaCoCo XML 里找到目标源文件，然后看 issue 的 `start_line` 到 `end_line` 这些行被覆盖了多少。  
同时也会记录目标文件整体覆盖情况。  
我更关注目标行，是因为这次重构真正会影响的是 issue 对应区域，而不是整个类的平均覆盖率。

### Q8：覆盖率阈值是多少？怎么定的？如果目标代码本来就很难测怎么办？

代码里目标区域覆盖率阈值是 80%。  
这个不是说 80% 就绝对安全，而是一个工程门槛：低于这个就认为重构风险比较高，需要先补行为锁定测试。  
如果目标代码本来就很难测，比如强依赖外部系统或全局状态，那自动重构就应该保守。  
这种情况下宁可停下来让人工补测试，也不要让 Agent 直接改。

### Q9：行为锁定测试是什么？和普通单元测试有什么区别？

行为锁定测试的目标不是证明业务设计有多完美，而是锁住当前可观察行为。  
它是在修改前生成并运行的，用来保证重构前后行为一致。  
普通单元测试可能是长期设计出来的业务测试；行为锁定测试更像重构前的安全网。  
它先保护“不改坏”，再谈代码结构怎么变好。

### Q10：测试 Agent 生成的测试怎么防止“假测试”？比如空断言、恒真断言、只测 mock 不测行为。

代码里有几层限制。  
生成测试必须包含目标类，必须有 `@Test`，必须有可观察断言或 mock verification。  
同时禁止 `@Disabled`、`@Ignore`、`assertTrue(true)`、`assertFalse(false)` 这类空测试或恒真断言。  
生成后还要跑 `test-compile`、完整测试、JaCoCo，不是文本上像测试就放行。

### Q11：生成的行为锁定测试是在修改前跑还是修改后跑？为什么一定要先在原始代码上跑通？

一定是修改前跑。  
因为行为锁定测试要证明它锁的是原始行为，而不是修改后的行为。  
如果修改后才写测试，就可能变成专门适配新实现的测试，失去防回归价值。  
所以代码里测试生成通过预检以后，才会进入修改 Agent 阶段。

### Q12：如果测试 Agent 生成的测试本身是错的，你怎么识别？会不会把错误行为锁死？

这个风险确实存在，不能说完全没有。  
我做的识别主要是工程约束：它必须在原始代码上编译通过、完整测试通过，并且产生目标文件覆盖。  
这能过滤掉很多明显错误的测试。  
但如果需求本身不清楚，测试也可能锁住错误行为，所以最终仍然需要 review 生成测试和 diff。

### Q13：修改 Agent 输入里包含什么？是原始 issue、重构计划、测试结果，还是完整仓库？

修改 Agent 的输入包含 issue、确认后的重构计划、允许修改文件、当前 attempt，以及上一轮验证反馈。  
它不是拿完整仓库自由发挥，而是通过只读工具按需读取目标文件、调用方和测试。  
真正写文件也必须调用 `apply_edits`，输出结构化行级编辑。  
这样它的自由度被限制在计划和工具边界里。

### Q14：修改 Agent 为什么不能自己写测试再自己验证？这样不是更简单吗？

确实更简单，但会回到单 Agent 自我验证的问题。  
如果修改 Agent 自己写测试，它可能写出刚好适配自己实现的测试。  
如果它自己验证，也容易忽略自己修改里的风险。  
所以我把测试生成放在修改前，把验证放在修改后，并且由不同 Agent 负责。

### Q15：修改 Agent 如果为了通过测试做了过拟合改动，比如只满足测试 case，不是真正重构，你怎么发现？

我不是只看测试过没过。  
验证 Agent 会看重构计划、真实 diff、coverage、静态检查结果和目标坏味道是否消除。  
如果只是为了过测试加特殊分支，但没有按计划重构，验证 Agent 应该拒绝。  
另外 patcher 也会限制文件范围和行级编辑，避免它为了过测试乱改一大片。

### Q16：修改 Agent 每轮失败后收到什么反馈？是完整日志、摘要，还是结构化错误？

收到的是验证结果里的结构化反馈。  
里面包括 status、命令结果、coverage、真实 diff、issues、suggestions 和 message。  
下一轮修改时，这个 `VerificationResult` 会作为 `verification_feedback` 传给 Modifier。  
它不是重新盲改，而是针对验证 Agent 给出的具体问题修。

### Q17：重试次数怎么定？为什么不是无限重试？

代码里重试次数可以通过 `--max-repair-attempts` 配，默认是有限次数。  
不做无限重试，是因为 LLM 失败很多时候不是多试几次就能解决，反而可能越改越偏。  
每轮失败后会回滚生产文件，再带着反馈重试。  
到上限还不通过，就最终回滚，保守退出。

### Q18：验证 Agent 和 Maven test / JaCoCo 的区别是什么？机器测试已经通过了，为什么还要 Agent 验证？

Maven test 和 JaCoCo 解决的是硬事实：能不能编译、测试过没过、覆盖够不够。  
验证 Agent 解决的是语义问题：这个 diff 是否符合重构计划，目标坏味道是否真的消除，有没有计划外修改。  
所以我把验证分两层：机器验证负责硬门槛，Agent 验证负责结合上下文做语义审查。  
测试通过只是必要条件，不是充分条件。

### Q19：验证 Agent 判断“目标坏味道是否消除”时看什么证据？真实 diff、重构计划、AST 指标，还是源码？

它会看重构计划、真实工作区 diff、源码、coverage 和命令结果。  
代码里 verifier 必须跑必需命令，必须 inspect diff，必须 get coverage assessment。  
如果没有调用这些证据工具，它的最终决策会被 validator 拒绝。  
所以不是让它凭感觉说通过，而是让它基于工具证据判断。

### Q20：验证 Agent 会不会也有误判？如果验证 Agent 错拒或错放，怎么办？

会，验证 Agent 也是 LLM，不可能完全没有误判。  
所以硬条件不交给它自由判断，比如 compile/test 失败不能 approve，基础设施错误也会被拦住。  
如果错拒，最多进入下一轮或人工看证据；如果错放，还有真实 diff、覆盖和工作区范围检查降低风险。  
自动流程默认偏保守：没有 approve 就不算成功。

### Q21：验证失败反馈给修改 Agent 时，如何避免下一轮改得越来越偏？

每次修复前会先把生产文件从快照回滚，保留已经通过预检的生成测试。  
这样第二轮不是在第一轮错误 diff 上继续堆补丁，而是回到干净基线重新改。  
同时修改仍然只能改计划里的白名单文件。  
这个设计能避免多轮修复越改越乱。

### Q22：验证 Agent 有没有权限修改代码？如果没有，它只能做什么？

没有。  
验证 Agent 的工具主要是读文件、搜索、inspect diff、跑验证命令和读覆盖率。  
它不能调用 `apply_edits`，也不能直接写文件。  
它只能 approve、reject、给 issues 和 suggestions。

### Q23：Pass@1 从 75.0% 到 88.3%，成功的定义是什么？

我的成功定义是：在不人工介入的情况下，最终通过编译、测试、覆盖检查和验证 Agent 审查，并且目标坏味道被消除。  
Pass@1 指的是第一次生产代码修改尝试就成功，不靠后续多轮修。  
测试生成属于修改前安全准备，不等于生产代码修复尝试。  
这个数字是项目评测集上的结果，我不会说成线上大盘。

### Q24：这个提升是在哪些任务、多少样本上测的？是否覆盖不同坏味道类型？

这个可以按项目评测集口径讲。  
任务覆盖了主要坏味道，比如长方法、重复代码、复杂条件、特性依恋等。  
样本来自开源 Java Maven 项目和人工复核过的变体。  
样本规模不是特别大，所以我会强调它是项目内 benchmark，主要证明设计方向有效。

### Q25：Baseline 是什么？单 Agent 是同一个模型、同样工具、同样预算吗？

Baseline 是一个单 Agent 同时负责修改和验证自己的结果。  
公平对比时应该保持同一个模型、同一批任务、相近工具权限和预算。  
区别主要在职责拆分：多 Agent 把测试、修改和验证隔离开。  
我不会把模型差异造成的提升算到架构设计上。

### Q26：Pass@1 是只算第一次修改成功，还是允许测试 Agent 先补测试后再算？

我的口径是允许测试 Agent 先补测试，因为这是修改前安全准备。  
Pass@1 统计的是第一次生产代码修改能不能通过完整验证闭环。  
如果测试生成阶段就失败，那任务不会进入自动修改，也不能算成功。  
这样统计更符合安全重构流程。

### Q27：多 Agent 会增加耗时和 Token，你怎么证明成功率提升值得这个成本？

这要看目标是什么。  
如果目标是快速生成 demo，单 Agent 成本更低；但我的目标是自动重构，错误修改的成本更高。  
多 Agent 多花一些 Token，换来的是更高成功率、更清晰的失败原因和可回滚链路。  
所以这个设计是安全优先，不是最低成本优先。

### Q28：有没有看失败原因分布？比如测试生成失败、修改失败、验证失败分别占多少？

代码里有这个分析基础。  
每轮都会落 `modifier.json`、`verifier.json`、`feedback.json`，preflight 也会记录覆盖和测试生成结果。  
常见失败原因包括覆盖不足、测试生成不通过、修改编辑不合法、Maven 测试失败、验证 Agent 拒绝 diff。  
目前我会说有分类记录能力，但不把它包装成大规模统计系统。

### Q29：为什么不用传统 CI + 静态规则验证，而要加验证 Agent？

传统 CI 和静态规则很重要，但它们主要回答硬条件。  
比如能不能编译、测试有没有失败、规则是否还触发。  
但重构还有语义判断：这个 diff 是否真的符合计划，有没有引入新的职责问题，目标坏味道是否真正消除。  
所以我不是替代 CI，而是在 CI 结果上加一层语义验证。

### Q30：多 Agent 之间的数据结构怎么设计？如果全靠自然语言传递，会不会不稳定？

如果全靠自然语言传，确实会不稳定。  
所以我用结构化对象传递核心状态，比如 `RefactorPlan`、`CoverageAssessment`、`PreModificationResult`、`VerificationResult`。  
自然语言 summary 只是辅助说明，真正进入下一阶段的是 JSON 化结果和工具证据。  
这也是多 Agent 可控的关键。

### Q31：这套机制对小改动会不会太重？有没有轻量模式？

会偏重。  
这套机制更适合自动重构、跨方法修改、覆盖风险不确定的场景。  
如果只是改注释、改很小的命名，完全可以走轻量模式，比如跳过测试生成，只跑基础 compile/test 和 diff 检查。  
当前项目主打安全闭环，所以默认保守。

### Q32：如果项目本身测试不稳定、JaCoCo 报告不准，流程怎么降级？

如果原始代码 compile/test 都不过，代码里会直接禁止自动修改。  
如果修改前 JaCoCo 命令失败，也会阻断，不会假装有覆盖。  
修改后覆盖报告失败可以降级为 warning，但验证 Agent 要结合证据判断。  
如果项目测试本身 flaky，我会先要求稳定测试基线，再启用自动重构。

### Q33：行为锁定测试会不会增加维护成本？成功重构后这些测试保留还是删除？

会增加一些维护成本。  
但它也提供了后续回归保护，所以当前实现倾向于保留生成测试。  
修复轮之间也会保留已经通过预检的生成测试，避免每轮重复生成。  
更产品化时可以让用户选择：保留、转人工 review 后合并，或者重构完成后删除。

### Q34：这套设计最容易失败的边界场景是什么？

第一类是目标代码很难测，比如强依赖外部系统、时间、随机数、全局状态。  
第二类是项目构建本身不稳定，Maven 或 JaCoCo 跑不起来。  
第三类是重构需要大范围跨文件、改公开 API，这会超出当前白名单和行级编辑的安全边界。  
这些情况下我会宁可拒绝自动改。

### Q35：如果测试 Agent 生成了错误测试，修改 Agent 按错误测试改，验证 Agent 也通过了，怎么兜底？

这个风险不能完全消除。  
兜底主要是三层：测试必须在原始代码上通过，避免为修改后行为定制；验证 Agent 要看真实 diff 和计划，不只看测试；最终产物仍然建议人工 review。  
我不会说它能证明测试语义 100% 正确。  
它解决的是“无覆盖直接改”的风险，而不是替代需求判断。

### Q36：如果验证 Agent 一直拒绝，但 Maven 测试都通过，你信谁？

我会看拒绝证据。  
如果 Maven 过了，但验证 Agent 指出计划外文件变化、目标坏味道没消除、coverage 不足，那我会信验证 Agent，拒绝自动重构。  
如果验证 Agent 没有证据地拒绝，那就是验证 Agent 自身质量问题，需要人工查看 `verifier.json` 和真实 diff。  
自动流程里默认保守：没有 approve 就不合并。

### Q37：如果修改 Agent 第一次失败，第二次为了过验证做了更大范围改动，怎么限制越界？

首先，Modifier 只能改 plan 里的 `files_to_modify`，patcher 会校验白名单、路径和行号。  
其次，每轮修复前会回滚生产文件，避免在错误补丁上继续扩大。  
最后，验证阶段会检查真实 workspace manifest，发现计划外文件变化就失败。  
所以它想扩大范围也会被工具和验证拦住。

### Q38：面试官说：“这不就是多个 prompt 串起来，没什么架构价值。”你怎么反驳？

如果只是多个 prompt，那确实没什么架构价值。  
我的设计重点不是 prompt 数量，而是角色隔离、工具权限隔离、结构化数据契约和真实工作区验证。  
测试 Agent 只能写新测试，修改 Agent 只能通过 patcher 改白名单文件，验证 Agent 不能写代码但必须跑证据工具。  
所以它是一个有边界、有审计、有回滚的工作流，不是简单 prompt 串联。

### Q39：单 Agent 成功率 75% 已经不低，为什么要复杂化到三个 Agent？

75% 看起来不低，但自动重构场景里，剩下 25% 的失败成本很高。  
失败可能不是简单报错，而是误改、越界改、测试没覆盖就改。  
多 Agent 把风险拆开：测试先锁行为，修改只做最小编辑，验证专门挑错。  
如果目标是安全重构，我愿意用一些复杂度换成功率和可恢复性。

### Q40：如果让你上线到团队 CI 里，你会默认启用这套多 Agent 自动重构吗？为什么？

我不会一上来默认自动改主分支。  
更合理的是先作为建议模式：扫描候选、生成计划、跑测试和验证，最后产出 PR 或 patch 给人 review。  
等在团队代码库里验证足够稳定后，再对低风险类型开启半自动。  
自动重构应该渐进上线，不能一开始就完全无人值守。

## 3. 亮点 3 口语逐字稿（结构化编辑与越界防护：Q1-Q50）

### Q1：LLM 越界修改具体指什么？你在项目里遇到过哪些越界行为？

我说的越界不只是改了错误代码，还包括改了计划外文件、扩大修改范围、顺手调整公开 API，或者输出整文件导致大量无关格式变化。  
LLM 的目标通常是“尽快完成任务”，所以它有时会顺手修调用方、改测试、重命名公共方法。单看结果可能合理，但这些变化没有经过计划确认。  
因此我把“允许改什么”从 prompt 建议提升成 patcher 强制执行的安全边界。

### Q2：为什么 LLM 容易越界修改？是 prompt 不够强，还是模型天然不适合直接写文件？

不完全是 prompt 写得不够强。LLM 是概率模型，它擅长提出修改方案，但不适合作为最终文件系统权限主体。  
只靠一句“不要改其他文件”，模型仍可能为了让代码编译而扩大范围。  
我的设计是把 LLM 降到决策和建议层，真正写文件的是本地 patcher；prompt 管意图，程序校验管权限。

### Q3：为什么选择“结构化行级编辑”，而不是让 LLM 输出 unified diff 或整文件内容？

整文件输出容易覆盖遗漏内容，也会引入大量格式噪音。自由 diff 虽然紧凑，但解析容错、上下文匹配和业务白名单校验更复杂。  
结构化行级编辑只有 `file_path`、`start_line`、`end_line`、`replacement`，本地程序很容易校验路径、范围和目标文件。  
核心亮点是把不可控的自然语言生成，收敛成一个小而可验证的写入协议。

### Q4：结构化行级编辑的 schema 长什么样？包含哪些字段？

代码里一次 `apply_edits` 接收完整 edits 数组和 explanation。每条 edit 包含 `file_path`、`start_line`、`end_line` 和 `replacement`。  
`file_path` 决定目标文件，起止行决定替换范围，replacement 是新内容，explanation 用于审计说明。  
Agent 只有成功调用 `apply_edits` 后，才能返回 `status=applied`，不能只在文本里声称自己改完了。

### Q5：这种方式会不会限制 LLM 的修改能力？比如跨多处重构、移动方法、拆类怎么办？

会限制，而且这是有意的安全取舍。  
它适合范围明确、能拆成有限行级替换的重构；跨很多文件、创建复杂新类型、调整公共 API 的任务，当前实现会更保守。  
我的原则不是让 Agent 什么都能改，而是只自动化“边界清楚、能验证、能回滚”的改动。复杂重构可以拆计划、分阶段执行，或者转人工 review。

### Q6：文件白名单从哪里来？是扫描 issue 生成的，还是重构计划里生成的？

最终白名单来自用户确认后的 `RefactorPlan.files_to_modify`。  
扫描 issue 提供目标文件和证据，规划阶段结合源码、调用方和风险生成计划，用户确认后才成为 patcher 的授权范围。  
这样扫描结果只是问题线索，计划才是修改授权，检测和执行之间有明确的权限边界。

### Q7：如果 LLM 认为必须改白名单外的文件怎么办？

它不能直接改。代码里只要 edit 的文件不在 plan 中，patcher 就抛 `PatchError`。  
正确做法是返回 `cannot_apply`，说明为什么当前计划不足，再让用户重新生成或确认扩大后的计划。  
这体现了“模型不能自行扩权”：技术上需要改，不等于它有权限改。

### Q8：白名单校验是在 LLM 输出前做，还是 patch 应用前做？

两层都有。prompt 和 tool schema 会把 allowed files 提供给模型，尽量让它一开始就别输出越界内容。  
但真正可信的是应用前的本地校验：`_changes_from_llm_edits` 会再次检查文件是否属于 `files_to_modify`。  
前一层减少失败，后一层才是安全门禁。

### Q9：只靠 prompt 告诉 LLM“不要改别的文件”不行吗？为什么还要本地校验？

不行。prompt 是软约束，模型可能误解、遗忘，也可能受到仓库中恶意文本影响。  
本地校验是确定性约束，同样输入一定得到同样结果。  
安全设计里不能让概率模型同时拥有决策权和最终写权限，所以写入边界必须由非 LLM 代码执行。

### Q10：如果测试文件是修改前生成的，它算不算白名单？怎么区分生产文件和生成测试？

生成测试走独立通道，不属于 Modifier 的生产文件白名单。  
Test Generator 只能在根据目标源文件推导出的 `src/test/java` 候选路径中新建测试，而且不能覆盖已有测试。  
后续验证和 workspace 检查会把计划文件与 `generated_tests` 一起视为允许变化，但 Modifier 本身不能借机修改测试。

### Q11：路径边界主要防什么？路径穿越、绝对路径、`.git`、`target` 这些怎么处理？

主要防路径穿越、绝对路径、写构建产物或仓库元数据。  
patcher 会拒绝绝对路径以及包含 `.git`、`target`、`build`、`.gradle`、`node_modules` 的路径；然后把目标 resolve 成真实路径，再确认它仍位于项目根目录内。  
所以不是简单做字符串前缀判断，而是规范化后再做根目录边界校验。

### Q12：Windows 和 Linux 路径分隔符不同，你是怎么统一处理的？

进入校验前会把反斜杠统一成正斜杠，并去掉多余空白和开头斜杠，内部统一使用相对路径。  
真正访问文件时再交给 `pathlib.Path` 解析。  
这样业务层的白名单比较不依赖 Windows 或 Linux 的分隔符形式。

### Q13：如果 LLM 输出 `../xxx` 或绝对路径，会发生什么？

绝对路径会在 `_resolve_allowed_file` 被拒绝。`../xxx` 即使字符串形式通过前置规范化，resolve 后也会落到项目根目录之外，`relative_to(root)` 检查会失败。  
最终结果是 `PatchError`，不会写入文件。  
这里我依赖的是解析后的真实路径边界，而不是相信模型给的字符串。

### Q14：如果 LLM 输出软链接路径，可能越出项目根目录吗？有没有考虑？

有考虑一部分。`Path.resolve()` 会解析软链接，如果链接目标在项目根目录外，后续 `relative_to(root)` 会拒绝。  
工作区 manifest 也不会跟随目录软链接，并会把文件软链接目标作为哈希内容记录。  
更严格的生产版本还可以直接禁止修改任何 symlink；当前实现的核心防线是 resolve 后的根目录检查。

### Q15：为什么补丁工具要自己做路径校验，而不是依赖 git 或文件系统报错？

因为 git 和文件系统只判断“能不能写”，不判断“业务上允不允许写”。  
一个计划外文件可能完全合法、也存在，但 Agent 仍然不该修改它。  
自定义 patcher 能同时表达项目根边界、忽略目录、计划白名单和文件必须存在这些业务安全规则。

### Q16：行号范围怎么校验？`start_line`、`end_line` 必须满足什么条件？

要求 `start_line >= 1`，`end_line >= start_line`，并且 `end_line` 不能超过当前文件总行数。  
任何一个条件不满足都会抛 `PatchError`，不进入写入阶段。  
这保证了模型不能通过负数、倒序范围或文件外行号制造不可预测替换。

### Q17：如果 LLM 输出的行号和当前文件已经不匹配怎么办？

当前实现会按应用当时文件内容校验范围。如果行号已经超界会直接拒绝；如果仍合法但语义位置漂移，后面的 AST、编译测试和 verifier 会继续拦截。  
任务开始前会创建快照，多轮修复前也会回滚，尽量避免行号基于不同版本漂移。  
更强的优化可以给 edit 增加 `expected_before` 或内容哈希，做乐观锁校验。

### Q18：多个 edit 修改同一个文件时，行号偏移怎么处理？

这里要如实说明：当前实现最稳妥的使用方式是，一个文件由一次完整替换表达，或者由模型合并成一个 edit。  
现有 `_changes_from_llm_edits` 会分别基于原文件生成 change，没有实现行号偏移重算；同文件多 edit 可能被后一个结果覆盖。  
这是已知边界。下一步应该先按文件聚合，检查范围，再按行号倒序应用，或者要求 schema 每个文件只允许一条 edit。

### Q19：如果两个 edit 的范围重叠怎么办？

当前代码没有显式的 overlap 检测，所以我不会说已经解决。  
安全口径是要求 Agent 在一次 `apply_edits` 前合并重叠修改，并尽量一个文件一个完整 edit。  
产品化时应该在 patcher 增加区间排序和重叠拒绝，这属于明确可补的确定性校验，不应该交给 prompt 保证。

### Q20：为什么行级编辑比自由 diff 更容易校验？

因为行级编辑字段固定，不需要猜 diff hunk、上下文匹配和 patch fuzz。  
本地程序可以在写入前直接判断文件、行号和 replacement，并明确计算 before/after。  
自由 diff 更灵活，但解析失败和模糊匹配会增加安全边界；这里我优先选择可验证性。

### Q21：补丁应用是一次性事务吗？中间一个文件失败时，前面已经写入的文件怎么恢复？

它实现的是应用级事务。写入前每个 `PatchChange` 都保存 before_text；写入、回读或 AST 校验任何一步失败，catch 分支会把已经写过的文件恢复成 before_text。  
所以不会出现前两个文件改了、第三个失败后把半成品留在工作区。  
这不是数据库事务，但具备“全部通过才接受，否则恢复”的事务语义。

### Q22：你是先生成 changes 再 apply，还是边解析边写文件？

先生成 `PatchChange`，再统一 apply。  
生成阶段负责检查计划、文件白名单和行号，并计算完整 before_text/after_text；应用阶段才写磁盘、回读比对并跑 AST 校验。  
把“决策”与“副作用”拆开，可以在真正写入前发现大部分问题。

### Q23：修改前有没有快照？快照保存什么？

有，而且是在测试生成和生产修改之前创建不可变初始快照。  
快照保存任务和 issue ID、Git HEAD、任务前 git status、计划文件列表、工作区 manifest，以及计划文件的原始副本、大小和 SHA-256。  
它既用于恢复，也用于后面重建真实 diff 和检查计划外工作区变化。

### Q24：应用后如何证明真实工作区确实只改了计划内文件？

验证阶段不会相信 Agent 声称的 changed_files，而是重新抓取 workspace manifest，与任务初始 manifest 做哈希比较。  
允许变化的只有 `plan.files_to_modify` 和受控生成的测试文件；其他文件变化都会形成 finding，使验证失败。  
这解决了“patch 记录看起来正常，但真实工作区还有额外修改”的问题。

### Q25：如果 patcher 应用成功，但文件内容不是预期 after_text，怎么处理？

写完以后会重新读取每个目标文件，并与计算出的 `after_text` 精确比较。  
不一致就抛 `PatchError`，然后恢复所有已经写入的文件。  
所以“write_text 没报错”还不算成功，必须回读一致并通过 AST 校验。

### Q26：为什么应用后还要跑 JavaParser？Maven compile 不能发现语法问题吗？

Maven compile 能发现语法和类型问题，但启动更重，而且它只回答能不能编译，不方便直接比较修改前后的结构契约。  
JavaParser 可以在 patch 落盘后立刻解析，并比较类声明和非 private 方法签名。  
它是快速的局部结构门禁，Maven compile/test 则在后续 verifier 中做更完整的工程验证，两者不是替代关系。

### Q27：JavaParser 校验是在每次 patch 后跑，还是最终验证阶段跑？

每次 `apply_changes` 写入后就立即跑 `AstPatchValidator`。  
也就是说，结构不合法的补丁不会等到最终 verifier 才发现，而是在 patcher 内部当场恢复。  
之后最终验证还会再跑 Maven compile、test、coverage 和真实 diff，形成分层防线。

### Q28：JavaParser 解析失败时怎么处理？

无论是修改前快照还是修改后文件解析失败，validator 都会产生 finding。  
patcher 看到 finding 就把它转成 `PatchError`，恢复本轮写入。  
不会因为解析器失败就跳过检查继续接受补丁，因为那相当于安全门禁失效时默认放行。

### Q29：AST 校验除了语法，还检查了哪些结构变化？

目前检查两类核心结构。  
第一，类声明集合是否变化，包括类的 kind 和名称；第二，所有非 private 方法签名集合是否变化。  
这样既能抓语法解析失败，也能抓意外新增、删除类，以及公开或包可见方法签名变化。

### Q30：为什么要检查公开 API 变化？哪些变化算公开 API 变化？

重构原则上应该保持外部行为和调用契约稳定。公共方法一旦改名、改参数或删除，调用方即使当前测试没覆盖，也可能被破坏。  
代码实现得更保守：检查的是所有非 private 方法，不只是 public，也包括 protected 和 package-private。  
比较内容是方法 signature 集合，出现 removed 或 added 都会拒绝。

### Q31：private 方法签名变化允许吗？public/protected 方法变化允许吗？

private 方法签名允许变化，因为提取方法、重命名私有 helper 本来就是常见重构。  
非 private 方法签名默认不允许变化，包括 public、protected 和 package-private。  
这体现了内部实现可以重组，但对外契约默认稳定的设计原则。

### Q32：如果重构本来就需要改公开 API，怎么办？

当前自动化安全路径不支持，AST validator 会直接拒绝。  
虽然 plan 可以描述风险和范围，但代码里还没有“用户明确授权后放行某个 API 变化”的机制，所以不能包装成已有能力。  
合理扩展是给计划增加 `allowed_api_changes`，记录精确签名前后值，并要求用户二次确认；在那之前应转人工执行。

### Q33：“校验失败自动恢复原文件”具体恢复到什么状态？

patch 应用阶段失败，会恢复到本轮应用前的 `before_text`。  
在第一次尝试里它就是任务开始时的生产文件；修复轮开始前又会先从初始快照恢复，所以每轮基线也是干净的。  
最终多轮验证失败，则由 TaskRollbacker 恢复任务初始快照，并处理受控生成测试。

### Q34：恢复只恢复当前文件，还是整个任务工作区？

分两层。patcher 内部失败时，恢复本次 changes 涉及的所有已写文件；不是只恢复报错的那个文件。  
最终验证失败或重试耗尽时，任务级 rollback 会恢复快照里的全部计划文件。  
一个是局部事务恢复，一个是任务级回滚。

### Q35：patch 应用失败和最终验证失败，回滚策略有什么区别？

patch 应用失败说明写入、回读或 AST 门禁没过，patcher 立即恢复本轮 before_text，通常连 verifier 都不会进入。  
最终验证失败说明 patch 在结构上合法，但编译、测试、覆盖、语义或工作区检查没通过。此时可以把证据反馈给 Modifier 重试；耗尽后再从任务快照整体回滚。  
两类失败发生在不同层，恢复范围和是否允许修复循环也不同。

### Q36：如果恢复过程也失败怎么办？

patcher 写回失败会继续以 `PatchError` 暴露，不能声称恢复成功。任务级 rollback 也有 `RollbackError` 和 conflict 状态。  
手工回滚默认会先比较 after-state 哈希，发现任务后有人又改过同一文件就拒绝覆盖，除非明确 force。  
真正生产化还应增加紧急恢复报告和人工介入指引；安全系统最忌讳静默吞掉回滚失败。

### Q37：如何保证生成的行为锁定测试不被错误回滚或错误保留？

生成测试有独立清单和 SHA-256。修复轮之间回滚生产文件时会 `preserve_generated_tests=True`，并先检查测试内容没有被意外修改。  
最终任务失败时，完整回滚会删除这些新生成测试；如果测试已被外部修改，非 force 回滚会报告冲突。  
因此“中间轮保留、最终失败移除”是显式策略，不是碰巧留下。

### Q38：这种结构化编辑是不是相当于把 LLM 降级成“建议器”，真正写文件的是本地 patcher？

是的，我认为这正是设计亮点。  
LLM 擅长理解意图、生成局部修改方案，但不应该直接持有不受控的文件写权限。  
它输出受限 edit plan，本地 patcher 才是执行主体，负责权限、事务、AST 和恢复。模型负责智能，程序负责确定性。

### Q39：为什么不直接用 `git apply`？自己写 patcher 的必要性是什么？

`git apply` 擅长应用 diff，但它不了解我的业务授权。  
我还需要检查文件是否在 plan 白名单、路径是否安全、行号是否合法、修改后 API 是否稳定，以及失败后按任务快照恢复。  
这些不是普通 patch 工具自动提供的，所以自定义 patcher 是策略执行层，不只是另一个 diff 应用器。

### Q40：这种机制能防止恶意 prompt injection 吗？能防到什么程度？

它不能消灭 prompt injection，但能显著限制攻击后的写入能力。  
即使仓库文本诱导模型去改 `.git`、`target` 或计划外文件，本地白名单和路径边界仍会拒绝。  
它防的是越权副作用，不保证模型的语义判断永远正确；所以还需要只读工具隔离、真实 diff 验证和人工 review。

### Q41：如果 LLM 输出看似合法但语义错误的编辑，这套校验能发现吗？

不一定全部发现。路径、行号、语法和 API 都合法，不代表业务语义正确。  
这层主要解决“能不能安全写”，语义正确性由后续 Maven test、JaCoCo、行为锁定测试和 Verifier Agent 判断。  
设计上不是期待一个校验器包打天下，而是把风险拆成权限、结构、行为、语义四层分别处理。

### Q42：这套机制主要防“越界修改”，那怎么防“边界内错误修改”？

边界内错误靠后续验证闭环。  
修改前用覆盖和行为锁定测试建立基线；修改后跑 compile/test/coverage；Verifier 结合计划和真实 diff 判断目标是否达成；失败则回滚并带反馈重试。  
patcher 解决权限安全，verifier 解决结果正确性，两者职责不同。

### Q43：结构化行级编辑和 IDE refactoring API 相比有什么优劣？

IDE refactoring API 对重命名、移动方法这类语义操作更强，能自动更新引用，安全性通常更高；但它和具体 IDE、语言服务耦合较深。  
行级编辑通用、协议简单，方便 LLM 输出，也容易做审计和回滚，但语义能力弱一些。  
后续可以把“Rename Method”这类操作升级成结构化语义 refactoring tool，而不是都靠行级替换。

### Q44：如果代码格式化工具改了大量行，会不会被误判为越界？

如果格式化只发生在计划文件内，不算文件级越界，但会让 diff 变大，Verifier 可能认为不符合最小修改原则。  
如果格式化波及计划外文件，workspace manifest 会直接报错。  
因此自动流程里不应该默认运行全仓格式化；最好只格式化受控片段或计划文件，并让真实 diff 接受审查。

### Q45：你怎么衡量“越界修改”减少了？有没有统计过拒绝了多少不合法补丁？

当前代码会明确抛出白名单、路径、行号、回读、AST/API 等不同失败原因，测试也覆盖了计划外文件拒绝和回滚。  
但我没有一套足够大的线上统计，所以不会虚报百分比。  
合理指标包括越界 edit 拒绝数、计划外文件 finding 数、AST/API 拒绝数，以及人工 review 发现的漏拦截率。

### Q46：结构化行级编辑是否降低了 LLM 一次成功率？安全性和成功率怎么权衡？

可能会降低，尤其是大范围重构，因为模型必须把意图准确映射成行号和 replacement。  
但自动重构里，我更在意“失败可见”而不是“错误地成功”。一次失败可以重试或转人工，越界成功可能污染整个仓库。  
所以我的取舍是先把安全边界做硬，再通过更好的上下文、语义工具和反馈循环提升成功率。

### Q47：校验失败后是让 LLM 重试，还是直接终止？

要区分阶段。`apply_edits` 内部校验失败会把错误返回给 Modifier，它在当前 ReAct 轮里可以修正 edit 再调用；如果整个 Modifier 仍失败，任务会回滚退出。  
补丁成功但最终 Verifier 拒绝时，才进入有上限的修复循环。  
也就是说，局部格式错误即时修，行为或语义失败按轮次回滚后重试。

### Q48：最常见的 patch 失败原因是什么？路径、行号、AST，还是 API 变化？

项目现阶段没有大规模线上分布，我不会编一个精确排名。  
从机制上看，常见的是行号与当前源码不匹配、模型试图修改 plan 外文件、replacement 造成 JavaParser 解析失败，以及意外改变非 private 方法签名。  
这些失败会分别留下明确错误，后续可以按 attempt 记录统计并优化 prompt 或工具。

### Q49：这套机制对大文件、多文件重构性能怎么样？

路径和行号校验成本很低，主要开销是为计划 Java 文件分别运行修改前后 JavaParser，以及后续 Maven 验证。  
少量文件的重构可接受，但很多文件时会线性变慢，而且行级协议本身也更容易出错。  
优化方向是缓存修改前 AST、批量解析修改后文件，并对大范围重构拆成多个可独立验证的计划。

### Q50：如果让你继续优化，你会加强哪一层：schema、patcher、AST 校验，还是 verifier？

我会先加强 patcher 和 schema，因为这里还有明确的确定性收益。  
第一，增加 `expected_before` 或文件哈希，防行号漂移；第二，按文件聚合 edits，显式拒绝重叠区间；第三，增加受控的新文件操作；第四，给计划增加精确的 `allowed_api_changes`。  
然后再把常见重构升级成语义工具。这样不是继续堆 prompt，而是持续缩小 LLM 能犯错的空间。

  

## 8. 亮点 4 口语逐字稿（验证、修复循环与回滚）

### Q1：你为什么把验证、修复循环和回滚设计成一个完整闭环，而不是修改完只跑一次测试？

我把自动重构看成一个受控事务，不把 LLM 的“我改好了”当成事实。  
系统先建立修改前基线，再让 Modifier 写入，之后由 Verifier 基于真实工作区取证；失败时先恢复生产文件，再把结构化反馈交给下一轮。  
这样每一轮都从已知基线出发，不会让错误补丁层层叠加；超过重试上限后再做全量恢复。  
核心目标不是追求一次生成成功，而是保证失败可见、过程可审计、结果可恢复。

### Q2：这套架构里，LLM 验证和机器验证各自负责什么？

确定性工具负责不可协商的事实，例如 Maven 退出码、真实 diff、文件 SHA-256、路径范围、Java AST 和工作区完整性。  
LLM Verifier 负责机器不容易表达的语义判断，例如改动是否真正满足计划、是否保持意图、风险说明是否合理。  
所以不是让 LLM 替代测试，也不是只靠测试覆盖所有语义，而是让两类证据互补。  
最终 `approved` 必须同时满足 LLM 判断为真，以及代码层硬门禁没有任何阻断项。

### Q3：你说 `approved = LLM 判断 AND 代码硬门禁`，代码里具体怎么落地？

`VerifierAgent` 先接收模型输出的布尔值 `approved`，再计算 `_hard_verification_issues` 和基础设施错误。  
最终表达式本质上是 `bool(model approved) and not hard_issues and not infrastructure`，所以模型不能绕过编译、测试或完整性检查。  
硬问题包含必跑编译失败、带 JaCoCo 的测试失败，以及真实 diff 或 workspace 检查产生的静态 finding。  
JaCoCo report 命令失败目前主要形成 warning，但仍然必须由 LLM 明确批准，系统不会仅凭模型一句话跳过硬门禁。

### Q4：为什么要以真实工作区和确定性工具为事实源？

因为 LLM 看到的是上下文和工具返回，它可能遗漏文件，也可能错误概括自己刚才的修改。  
Verifier 的 `inspect_diff` 会从初始快照和当前磁盘重新构造 diff，workspace manifest 也会重新扫描整个受管工作区。  
Maven 和 JaCoCo 同样由本地进程实际执行，退出码和报告才是证据，不采信模型自报结果。  
这让模型负责解释事实，而不是创造事实。

### Q5：`patch.diff` 已经记录了修改，为什么验证时还要重建真实 diff？

`patch.diff` 是应用阶段生成的工件，适合审计和展示，但它不一定代表验证时磁盘上的最终状态。  
例如外部进程、人工编辑或后续工具可能在 patch 生成后继续改文件，所以 Verifier 会用 `snapshot.json` 里的 before copy 对比当前文件。  
重建出来的 unified diff 才是当轮决策使用的真实 diff。  
如果计划文件没有实际变化，代码还会产生“未发现计划内代码变化”的 finding，而不是把空操作当成功。

### Q6：快照、workspace manifest 和真实 diff 三者有什么区别，为什么缺一不可？

快照保存计划内生产文件的原始副本和 before SHA-256，主要服务于精确对比与回滚。  
workspace manifest 对未忽略工作区文件做 SHA-256，主要发现计划外新增、删除或修改。  
真实 diff 则把允许范围内到底改了什么展示给 Verifier 做语义判断。  
所以三者分别回答“怎么恢复”“有没有越界”“具体改了什么”，是互补关系，不是重复建设。

### Q7：`workspace_manifest` 到底记录了什么，忽略了哪些内容？

它从仓库根目录递归遍历文件，对普通文件按二进制内容计算 SHA-256，对符号链接则哈希链接目标。  
实现明确不跟随目录符号链接，并忽略 `.git`、`.paicli`、`target`、`build`、`node_modules`、虚拟环境和常见缓存目录。  
验证时重新捕获 manifest，与任务开始时的 manifest 比较，再扣除计划文件和已登记的生成测试。  
因此它保护的是源码和配置工作区，不把 Maven 产物或缓存变化误报成越界。

### Q8：全量 SHA-256 manifest 会不会拖慢大仓库？

会，这是当前实现必须诚实说明的成本。  
虽然按 1MB 分块读文件，并排除了依赖、构建产物和缓存目录，但它仍然会读取所有未忽略文件；在大仓、机械盘或海量小文件场景下可能成为明显开销。  
项目目前没有足够大规模统计，我不会编造耗时数字。  
后续可以考虑 Git index 增量、文件元数据预筛、持久化哈希缓存或只监控受影响模块，但这些都要权衡漏检风险。

### Q9：修改前为什么也要跑验证，不能直接修改后再测吗？

如果原始代码本来就编译失败或测试失败，修改后失败时就无法判断是不是本次重构引入的。  
`PreModificationVerifier` 会先跑编译、带 JaCoCo 的测试和 report，基础设施错误、基线失败、覆盖命令失败都会阻止生产代码修改。  
覆盖不足则转入行为锁定测试生成，而不是盲目继续。  
这一步建立的是可归因基线，也是后续自动回滚和修复成立的前提。

### Q10：修改前和修改后的 Maven 命令分别是什么？

修改前基线是 `mvn -q -DskipTests compile`、`mvn org.jacoco:jacoco-maven-plugin:prepare-agent test` 和 `mvn org.jacoco:jacoco-maven-plugin:report`。  
修改后的 Verifier 强制要求同样三条默认命令都被调用，最终硬门禁重点阻断 compile 和带 JaCoCo 的 test 失败。  
report 失败会影响覆盖证据并形成 warning，但不会被 `_hard_verification_issues` 单独当成硬失败。  
命令执行不经过 shell，并设置了 180 秒超时，输出只保留末尾 4000 个字符。

### Q11：为什么不是简单执行 `mvn test`，而要用 JaCoCo `prepare-agent test`？

普通测试通过只能说明断言没有失败，不能证明目标修改区域真的被执行过。  
`prepare-agent test` 在跑测试的同时生成覆盖执行数据，后续 report 再产出 XML，CoverageAnalyzer 才能映射到目标源文件和行号。  
这样验证不仅问“测试绿不绿”，还问“被重构的区域有没有证据被覆盖”。  
不过覆盖只是执行证据，不等于断言质量，所以它仍然要和真实 diff、静态门禁以及 LLM 语义审查组合。

### Q12：JaCoCo 覆盖率在代码里是怎么判定的？

CoverageAnalyzer 会搜索各模块下的 `**/target/site/jacoco/jacoco.xml`，再按目标源码所属模块优先排序。  
它用 package 和 sourcefile 拼成的路径做后缀匹配，然后统计 issue 起止行中 `ci > 0` 的行。  
当前项目内口径是目标区域覆盖比例达到 0.8 才算充分，否则要求补行为锁定测试。  
这个 0.8 是项目策略阈值，不是行业统一标准，也没有被包装成线上效果数据。

### Q13：多模块 Maven 项目里，JaCoCo 报告匹配有什么风险？

当前实现会优先选择目标源码所在模块的报告，这是一个合理启发式，但不是严格模块坐标绑定。  
真正匹配 sourcefile 时使用路径后缀，如果不同模块有相同 package 和同名类，仍然存在匹配到错误报告的风险。  
聚合报告、非标准 source root、重命名模块也可能让排序和匹配失准。  
所以上线前应补充 module GAV 或 reactor 模块映射，并为同名类和聚合 JaCoCo 建专门测试。

### Q14：覆盖不足时，测试生成 Agent 的触发条件是什么？

修改前如果没有 JaCoCo XML、目标区域覆盖不足，或者目标文件覆盖行数为零，PreModificationVerifier 会标记 `requires_test_generation`。  
Orchestrator 随后让 TestGeneratorAgent 只在原始生产代码上生成新的行为锁定测试。  
只有最新版本测试完成强制预检并让目标文件出现覆盖，才会形成可用 application。  
如果生成失败或基础设施失败，流程直接全量回滚，不允许继续改生产代码。

### Q15：测试提示里说“连续通过两次”，实际代码真的是同一测试命令跑两次吗？

不是，这里必须按真实代码纠正。  
实际预检依次执行 `mvn -q -DskipTests test-compile`、`mvn test`、`mvn org.jacoco:jacoco-maven-plugin:prepare-agent test` 和 JaCoCo report。  
所谓“两次”是 `mvn test` 与带 `prepare-agent` 的两条测试命令，不是同一条命令原样重复两次。  
因此它能提供两次测试执行证据，但不能严格宣称做了同命令、同环境的 flaky test 重复验证。

### Q16：自动生成测试为什么可以保留到下一轮修复，而生产修改必须回滚？

这些测试是在原始代码上生成并通过强制预检的，它们代表本次重构要守住的行为基线。  
当 Verifier 拒绝第一轮生产修改时，Orchestrator 用 `preserve_generated_tests=True` 强制恢复生产文件，但保留测试给下一轮继续使用。  
每轮前后还会核对生成测试的 SHA-256，防止 Modifier 或外部进程悄悄改弱 guard。  
这样反馈循环修的是生产实现，不是通过篡改测试来“修绿”结果。

### Q17：生成测试本身有哪些硬限制？

GeneratedTestFileManager 只允许在由目标 `src/main/java` 确定性映射出的两个候选路径里创建新文件，并拒绝覆盖已有测试。  
一次只能提交一到四个文件，但当前候选路径实际只有两个；内容上限是 100000 字符。  
测试必须引用目标类、包含 `@Test`，还要有可观察断言或 mock verify，并拒绝 disabled、ignore 和明显常量断言。  
这些是最低质量门槛，不代表自动生成测试一定具备完整业务语义。

### Q18：反馈协议是怎么设计的，Verifier 到 Modifier 传了什么？

AgentMessage 明确定义了 `TASK`、`RESULT`、`FEEDBACK`、`APPROVAL`、`REJECTION` 和 `ERROR` 类型。  
Verifier 拒绝后，Orchestrator 把完整 verification 内容包装成 `FEEDBACK`，同时把 VerificationResult 作为下一轮 Modifier 的 `verification_feedback`。  
里面包含命令结果、覆盖评估、真实 diff 摘要、静态 finding、issues 和 suggestions。  
所有消息和 attempt 工件都会落盘，所以不是靠一段不可追踪的聊天上下文传递状态。

### Q19：为什么收到失败反馈后要先回滚，再让 Modifier 重试？

如果直接在失败补丁上继续改，第二轮的输入会混入第一轮未知副作用，问题归因和 diff 都会越来越复杂。  
代码在 `attempt > 1` 时先强制回滚生产文件，同时保留已经验证过的生成测试，然后才启动下一轮 Modifier。  
Modifier 看到上轮反馈，但操作对象是干净的生产基线，所以它需要给出一套完整的新修改。  
这相当于“保留知识、不保留坏状态”，是修复循环最重要的稳定性设计。

### Q20：重试次数怎么控制，默认到底会尝试几轮？

`run_apply` 的 `max_repair_attempts` 默认是 2，并先用 `max(0, value)` 消除负数。  
Orchestrator 的总尝试次数是 `max_repair_attempts + 1`，也就是默认一轮初始修改加两轮修复，共三轮 Modifier/Verifier 尝试。  
每轮 ReAct 内部另外还有 token budget、停滞检测和默认 50 次硬迭代上限，防止单个 Agent 无限循环。  
这些是代码配置，不代表生产最佳值；实际值应结合仓库规模、成本和失败类型再校准。

### Q21：达到重试上限后系统做什么，是否可能留下半成品？

最后一轮仍未批准时，Orchestrator 调用 `_final_rollback`，用任务初始快照恢复所有计划生产文件，并删除本任务生成的测试。  
回滚结果会保存到任务目录，ApplyWorkflowResult 返回失败，CLI 退出码为 1。  
也就是说，中间修复轮可以保留 guard，但最终失败必须全量恢复到任务开始前。  
如果连回滚本身都失败，结果可能返回 `None`，这属于需要人工介入的严重边界，不能声称绝对不会残留。

### Q22：自动回滚和用户手动 `rollback` 在冲突处理上有什么差异？

自动修复前回滚和最终失败回滚都调用 `force=True`，目标是让自动流程确定性地回到任务基线。  
这意味着即使计划文件在任务期间又被人工修改，自动回滚也可能覆盖这些变化，冲突只会记录而不会阻止恢复。  
手动 `run_rollback` 则先用 `force=False` 检测冲突，发现当前 SHA 与任务 after state 不一致就停止，并要求用户确认后才强制覆盖。  
所以人工回滚有冲突保护，自动回滚偏向流程一致性；两者的风险取舍不同，不能混为一谈。

### Q23：回滚是怎么识别“任务之后又有人改过同一个文件”的？

Patcher 成功写入后会生成 `after_state.json`，记录每个生产文件的 `after_sha256`。  
非强制回滚时，TaskRollbacker 计算当前文件 SHA-256，并与 after state 比较；文件缺失或哈希不同都记为冲突。  
生成测试则使用 `generated_test_files.json` 里的 `generated_sha256` 做同样检查。  
这能识别内容级冲突，但不能合并双方修改，所以冲突后的选择仍然是放弃回滚或经确认强制覆盖。

### Q24：如果任务开始前 Git 工作区本来就不干净，回滚会不会把用户原有修改抹掉？

初始快照是在 apply 的任何预检和自动写入之前创建的，保存的是当时计划文件的真实内容，而不是 Git HEAD 内容。  
因此正常回滚恢复的是“任务开始前状态”，计划文件里原有的未提交修改也会被保留下来。  
快照还记录 HEAD、`git status --porcelain` 和 `user_changes_before_task`，便于审计。  
但任务执行期间对同一文件的新人工修改，在自动 force 回滚中仍可能被覆盖，这是当前并发边界。

### Q25：你说 patcher 是事务写入，多文件情况下真的原子吗？

严格说不是真正原子。  
代码按文件逐个 `write_text`，如果 Python 进程仍在运行并捕获到异常，会把已经写过的文件恢复成 change 里的 before_text。  
但如果写到一半时进程崩溃、机器断电或被强杀，异常处理来不及执行，就可能留下部分文件已写、部分未写的状态。  
所以这里更准确的说法是“进程内补偿式事务”，后续应通过临时文件、原子 rename、write-ahead journal 或启动恢复进一步加强。

### Q26：Patcher 在真正写文件前后有哪些确定性保护？

写前会检查计划至少包含一个允许文件、issue 文件必须在白名单内、路径不能越出项目根，也不能触碰 `.git`、`target`、`build` 等目录。  
LLM edits 还必须落在计划文件和合法行号范围内，空修改会被拒绝。  
写后会重新读取并核对期望内容，再用 JavaParser 比较类声明和非 private 方法签名，异常时执行补偿恢复。  
因此 LLM 提供编辑意图，本地 patcher 才拥有最终写入裁决权。

### Q27：真实 diff 和 AST 门禁分别能发现什么，为什么不能只留一个？

真实 diff 能看到文本层面的完整变化，也能发现计划文件无变化、文件缺失和快照缺项。  
AST 门禁则专门检查 Java 是否还能解析、类声明是否意外变化、非 private 方法签名是否发生变化。  
纯文本 diff 很难稳定判断 API 结构，AST 又不能判断业务语义和最小修改原则。  
所以 AST 是结构硬门禁，diff 是证据载体，Verifier 再做语义层判断。

### Q28：Verifier 为什么强制先调用命令、diff 和 coverage 工具，不能让模型自由选择证据吗？

如果完全自由，模型可能因为上下文偏差漏跑关键命令，却仍然输出 `approved=true`。  
`evidence_error` 要求三条默认验证命令都有结果，并要求调用 `inspect_diff` 和 `get_coverage_assessment`，否则最终 JSON 校验不通过。  
每次命令执行后还会使已有 diff、coverage 和 workspace 证据失效，迫使模型基于命令后的最新文件状态重新取证。  
这把“建议查证”升级成了协议级前置条件。

### Q29：为什么 Maven 命令执行不经过 shell，这解决了什么安全问题？

VerificationPipeline 用 `shlex.split` 得到参数序列，再把列表直接交给 `subprocess.run`，没有 `shell=True`。  
这样管道、重定向和命令拼接不会被 shell 二次解释，显著降低命令注入面。  
计划附加命令还必须以 `mvn`、`mvn.cmd`、`mvnw` 或 `mvnw.cmd` 开头，并拒绝 `|`、`;`、`&&`、重定向、`exec:`、`antrun:`、deploy、install 和 clean。  
它不是通用沙箱，但比让 LLM 任意执行字符串命令安全得多。

### Q30：这套 Maven 命令安全检查有没有绕过或兼容性边界？

有，当前检查本质上仍是字符串黑名单加可执行文件白名单，不是 Maven goal 的完整语法解析器。  
例如不同平台的参数解析、插件前缀变体和未列入黑名单但有副作用的 goal，都需要继续审计。  
另外 `shlex.split` 默认按 POSIX 语义解析，在 Windows 复杂引号场景可能和实际命令行规则不完全一致。  
所以现阶段适合受信计划内的受限验证命令，不应宣称已经实现强隔离命令沙箱。

### Q31：同一个 workspace 同时跑两个 apply 会怎样？

当前没有任务级文件锁、workspace 副本或 worktree 隔离，因此同 workspace 并发任务是不安全边界。  
两个任务可能互相改变 manifest、覆盖同一计划文件、让 after SHA 失真，甚至一个任务的 force 回滚覆盖另一个任务的成功结果。  
现有快照和冲突检测主要解决串行流程内的一致性，不等于并发控制。  
上线前应至少加仓库级互斥锁，更理想的是每个任务使用独立 Git worktree 或临时 workspace。

### Q32：多模块项目的 Maven 命令从哪里执行，会不会验证范围过大或过小？

当前所有验证命令都在仓库根目录执行，没有自动加 `-pl` 或 `-am`。  
如果根 POM 是完整 reactor，这通常会覆盖相关模块，但成本可能很高；如果目标模块不在根 reactor，验证又可能漏掉它。  
多模块 JaCoCo 还存在同包同类匹配风险，所以“根目录执行成功”不天然等于目标模块证据准确。  
后续应从项目检测结果和源码路径推导模块坐标，再决定全 reactor 与定向模块验证策略。

### Q33：验证流程的主要性能瓶颈在哪里，你会看哪些指标？

主要成本不是 LLM 输出 JSON，而是 Maven 编译测试、JaCoCo report、全工作区 SHA-256，以及每轮重新解析 Java AST。  
我会记录基线耗时、每条 Maven 命令耗时、manifest 扫描文件数和字节数、Agent 轮次、重试次数、diff 大小与回滚耗时。  
质量侧看首轮批准率、最终批准率、硬门禁拒绝原因、回滚成功率、人工复核驳回率和生成测试保留冲突数。  
这些目前是建议指标和项目内口径，仓库没有大规模线上统计，所以不能编造百分比或 P95。

### Q34：如果测试套件很慢，三轮修复会不会把成本放大到不可接受？

会，最坏情况下修改前基线、测试生成预检和每轮 Verifier 都会执行 Maven 与 JaCoCo，成本按轮次放大。  
当前 180 秒是单条命令超时，不是整个任务的统一时间预算，多模块大仓尤其容易受影响。  
短期可以按风险分层设置重试上限，并把超时、总时长和 token 成本纳入任务预算。  
长期可以做受影响模块选择、增量编译和测试选择，但最终高风险合入前仍应保留完整验证。

### Q35：压力题：如果 LLM 坚持批准，但 `mvn compile` 失败，系统最终会怎样？

系统一定不会批准。  
`_hard_verification_issues` 会把 compile 的非零退出码加入硬问题，最终 `approved` 即使收到模型的 true，也会因为 `not hard_issues` 不成立而变成 false。  
Verifier 会形成 rejection，未到上限就反馈并先回滚后重试，到上限则全量回滚。  
这正是“模型可以建议，确定性门禁拥有否决权”的具体体现。

### Q36：压力题：所有 Maven 命令都通过，但 LLM 认为改动偏离计划，会怎样？

也不会批准，因为最终结果仍然要求模型 `approved=true`。  
测试通过只能证明现有测试没有发现问题，不能证明重构目标、最小修改原则和设计意图已经满足。  
Verifier 可以把偏离点写进 issues 和 suggestions，Orchestrator 将其作为反馈交给下一轮 Modifier。  
所以这是双钥匙机制，机器绿不等于自动放行。

### Q37：压力题：JaCoCo report 失败，但编译和带探针测试都通过，为什么不是绝对硬失败？

当前后验证实现把 compile 和 `prepare-agent test` 设为硬门禁，而 report 失败进入 warning 语义。  
原因是测试行为证据仍然存在，只是覆盖分析不可用，代码允许 LLM 在风险可接受时给出批准。  
但修改前基线更严格，report 失败会标记 `coverage_unavailable` 并禁止开始自动修改。  
这是当前真实策略，不代表永远最优；高风险生产场景可以把 report 失败也升级为硬失败。

### Q38：压力题：Verifier 运行过程中有人改了计划外文件，能发现吗？

只要该文件不在 manifest 忽略范围内，workspace 检查会比较任务初始 manifest 和当前 manifest，并产生计划外变化 finding。  
这个 finding 会进入 `static_findings`，同时也进入硬验证问题，因此模型无法批准。  
但检查是离散时点，不是持续文件监控；如果检查后、最终返回前又发生竞态，当前实现没有锁来彻底消除窗口。  
这再次说明 manifest 是完整性证据，不是并发隔离机制。

### Q39：你会怎么把这套能力安全上线，而不是一次性全量放开？

我会先用 shadow mode，只生成计划、快照、真实 diff 和验证报告，不自动写生产文件，用人工结果校准门禁。  
第二阶段只开放低风险、小 diff、单模块任务，并要求人工确认 apply 和最终 review；同时建立失败分类、回滚演练和告警。  
第三阶段再按仓库、规则类型和风险等级逐步扩大，遇到回滚失败、并发冲突或覆盖证据异常立即熔断。  
当前项目没有大规模上线数据，这里是建议策略，不会包装成已经验证过的生产收益。

### Q40：如果让你继续演进这个闭环，最优先补哪几个架构缺口？

第一优先是任务隔离，用仓库锁或独立 worktree 解决同 workspace 并发污染；第二是把多文件写升级为可崩溃恢复的真正事务协议。  
第三是让 JaCoCo 绑定明确模块坐标，解决多模块同名源码和聚合报告风险；第四是给自动 force 回滚增加租约、任务所有权和外部修改保护。  
然后再优化 manifest 增量哈希、模块级测试选择和全局时间预算，降低大仓成本。  
我的判断是先补正确性和隔离，再追求吞吐，因为自动重构最危险的不是慢，而是错误地成功。

## 9. 亮点 5 口语逐字稿（三层记忆、检索注入与历史压缩：Q1-Q50）

### Q1：请你从架构设计角度完整介绍一下，为什么这套系统要设计成稳定项目规则、跨会话长期事实和当前会话历史三层记忆，而不是只保留一份完整聊天记录？

我把记忆拆成三层，是因为三类信息的稳定性、生命周期和使用方式完全不同。  
PAI.md 保存稳定项目规则，长期记忆保存用户显式确认的跨会话事实，当前 history 和 short_term 则承载本次会话状态。  
如果全部混在聊天记录里，跨会话信息会丢失，旧对话还会持续挤占上下文窗口。  
三层设计的核心价值，是让稳定规则始终可见、长期事实按任务检索、临时历史按预算压缩。

### Q2：请你逐层说明这三层记忆各自负责什么、生命周期多长、存在哪里，以及它们之间是怎样协作的？

第一层是 PAI.md 项目记忆，存储在用户配置目录或项目目录的 Markdown 文件里，每次构造 prompt 时实时读取，适合长期稳定的项目约束。  
第二层是长期记忆，默认落在 `~/.paicli-py/memory/long_term_memory.json`，跨进程启动和跨会话保留，但只有显式保存的事实才会进入。  
第三层是当前会话，真实 LLM 消息放在内存中的 `history`，旁路的 `ConversationMemory` 也在内存中，会话结束后不会持久化。  
发起请求时，系统把 PAI.md 和按当前任务检索到的长期事实注入 system prompt，再带上当前 history 调用模型。

### Q3：PAI.md 这一层解决的核心问题是什么，它与长期记忆中的 fact 有什么本质区别？

PAI.md 解决的是版本化、可审查的稳定项目规则，例如构建命令、架构边界和明确的禁止事项。  
它是文件事实来源，可以由团队直接编辑并随项目管理，而且每次 prompt 都会重新读取。  
长期 fact 更适合用户偏好或需要跨会话记住的离散事实，由 `/save`、`save_memory` 或 `store_fact` 显式写入 JSON。  
所以前者强调项目规则的可见性和可维护性，后者强调跨会话事实的按需召回。

### Q4：系统到底会从哪些位置加载 PAI.md，这五个来源的完整顺序是什么，为什么顺序值得关注？

代码按固定顺序读取五个来源：用户配置目录的 `PAI.md`、项目根目录的 `PAI.md`、项目 `.paicli/PAI.md`、项目根目录的 `PAI.local.md`、项目 `.paicli/PAI.local.md`。  
存在且非空的来源都会按这个顺序拼接，不是后一个文件覆盖前一个文件。  
这个顺序让全局个人规则先出现，再进入项目规则和本地补充规则。  
当前没有显式的冲突解析或优先级合并，因此规则互相矛盾时仍要靠内容治理和模型理解。

### Q5：PAI.md 的 `@import` 是怎样实现的，你如何防止它通过路径穿越读取项目外的敏感文件？

只有形如 `@relative/path.md`、不含空格的整行才会被识别为 import，而且绝对路径和包含 `..` 路径段的候选不会作为导入执行。  
递归读取前还会对路径做 `resolve`，并要求结果仍位于当前来源对应的 `import_root` 内。  
用户级 PAI 只能导入用户配置目录内文件，项目级来源只能导入项目根目录内文件，同时用栈检测循环依赖。  
这构成了路径沙盒，但它只是文件读取边界，不是对 Markdown 内容本身的可信性认证。

### Q6：PAI.md 的导入深度和内容大小有什么限制，超限时系统的真实行为是什么？

导入最大深度是 3，继续向下的导入会被跳过并记录 warning，循环导入也会被跳过。  
五个来源及其导入内容拼接后，总预算最多是 24,000 个字符，不是 24,000 个精确 token。  
超过预算时，代码从尾部截断并追加“内容已按 24000 字符预算截断”的提示。  
读取失败同样只记录 warning 并返回空内容，不会阻断主任务。

### Q7：长期记忆的数据从哪里来，系统会不会自动从所有对话中抽取用户事实并永久保存？

不会，当前实现明确采用显式保存，而不是自动抽取所有对话。  
用户可以通过 `/save`、脚本化 `run_save`，或者在明确要求记住事实时由 `save_memory` 工具调用 `store_fact`。  
普通 user、assistant 和 tool 消息只进入当前会话内存，不会自动写入长期 JSON。  
这个选择降低了误记忆和隐私扩散风险，但也意味着用户没有显式保存的事实不会跨会话保留。

### Q8：为什么长期记忆采用显式 `store_fact` 或 `/save`，而不是让模型自动判断哪些内容值得保存？

自动抽取看起来省事，但模型可能把猜测、临时指令甚至工具噪声误当成长期事实。  
当前设计要求用户显式表达“记住”，chat 的 system prompt 也要求只在用户明确请求时调用 `save_memory`。  
这样保存动作更可解释，也便于通过 `/memory list`、`search`、`delete` 和 `clear` 管理。  
代价是召回率依赖用户操作，这是有意选择的安全边界，不应包装成全自动记忆。

### Q9：请解释长期记忆的 project scope 和 global scope 是怎样建模和过滤的？

每条长期记忆的 metadata 都带 scope，project 记忆还会记录项目根目录解析后的绝对路径作为 project key。  
按项目检索时，当前项目的 project 记忆和所有 global 记忆可见，其他项目的 project 记忆不可见。  
`store_fact` 默认使用 project scope，只有显式传入 global 或 `/save --global` 才保存为全局。  
这个模型很轻量，但项目移动路径后 project key 会变化，当前没有项目 ID 迁移机制。

### Q10：global scope 既然能跨项目复用，会带来什么污染风险，你会怎样向面试官说明这个取舍？

global 适合“默认用中文回答”这类真正跨项目的偏好，但它对所有项目都可见。  
如果把“项目使用 Java 17”误存成 global，它就可能在 Python 或其他 Java 项目中被召回，形成跨项目污染。  
当前代码没有权限、命名空间细分或自动冲突隔离，所以 scope 选择必须由用户或调用方负责。  
后续可以增加 workspace、organization 等层级，并在保存 global 前做显式确认。

### Q11：长期记忆默认存储在哪里，是否支持配置，文件内容是否加密？

默认路径是 `~/.paicli-py/memory/long_term_memory.json`，也可以通过 `PAICLI_PY_MEMORY_DIR` 环境变量修改目录。  
文件是 UTF-8 明文 JSON，保存 id、content、type、timestamp、metadata 和 `tokenCount`。  
当前没有加密、密钥管理和字段级脱敏，也没有内建权限系统。  
因此不能把密钥、个人敏感信息或生产凭据当作普通长期记忆保存。

### Q12：`MemoryEntry` 的数据模型包含哪些关键字段，token 数是怎样得到的？

`MemoryEntry` 包含 id、content、type、UTC timestamp、字符串 metadata 和 token_count，类型包括 conversation、fact、summary 与 tool result。  
如果创建时没有提供正数 token_count，代码会根据文本自动估算。  
中文大约按 1.5 个字符一个 token，其他字符大约按 4 个字符一个 token，并向上取整。  
这只是跨 provider 的容量估算，不是任何模型官方 tokenizer 的精确结果。

### Q13：长期记忆写 JSON 时为什么使用临时文件再 `replace`，它能保证什么，不能保证什么？

保存时先在同目录写 `long_term_memory.json.tmp`，完整序列化成功后再用 `replace` 替换正式文件。  
这样可以避免直接覆盖正式文件时进程中途退出，留下半截 JSON 的常见问题。  
它提升的是单次文件替换的原子性，不等于数据库事务，也没有 fsync、日志恢复或跨进程版本检查。  
因此它是轻量可靠性设计，不能宣称已经具备强持久化一致性。

### Q14：如果长期记忆文件损坏、读取失败或写盘失败，系统会怎样处理，会不会阻断主流程？

加载时如果 JSON 根不是数组、内容损坏或文件读取失败，代码记录 warning，并让当前实例以可解析到的内容或空集合继续工作。  
单条记录字段非法时会跳过该条，不会让整个应用崩溃。  
写盘的 `OSError` 也只记录 warning，不会阻断对话主流程。  
需要诚实说明的是，`store` 的内存更新可能已经发生，而 `_save` 吞掉写盘异常后调用方不一定知道持久化失败。

### Q15：`LongTermMemory` 为什么使用 `threading.RLock`，它具体保护了哪些操作？

`store`、`delete` 和 `clear` 都在同一个可重入锁内更新内存字典并触发保存。  
使用 RLock 可以保证同一进程内多个线程不会同时执行这些复合写操作，也允许未来同线程嵌套进入锁保护逻辑。  
它主要防止线程级的字典修改和文件写入交错。  
不过 `get_all` 和 `retrieve` 没有显式加锁，而且这个锁完全不覆盖其他进程。

### Q16：如果两个 CLI 进程同时写同一个长期记忆 JSON，RLock 能解决吗，最终可能发生什么？

不能，`threading.RLock` 只在当前 Python 进程内有效，不是文件锁或分布式锁。  
两个进程都可能从旧文件加载各自的内存快照，再分别写临时文件和 replace。  
后写入的进程可能覆盖先写入进程新增的内容，表现为 last-write-wins。  
要解决这个问题，需要跨进程文件锁、版本号加 compare-and-swap，或者迁移到支持事务的存储。

### Q17：当前长期记忆如何去重、处理冲突和过期事实，是否有 TTL 或可信度机制？

当前只在 `store` 时比较 content，内容完全相同才判定重复，而且这个去重跨 scope 生效。  
语义相同但措辞不同的事实不会去重，相互矛盾的事实也会同时保留。  
系统没有 TTL、自动冲突合并、版本冲突检测、事实可信度或来源权重。  
所以它目前是可管理的事实列表，不是具备知识治理能力的知识库。

### Q18：请你完整描述长期记忆的检索算法，它是不是 embedding 语义检索？

它不是 embedding，也没有向量库，本质是关键词和词袋匹配。  
查询先转小写并分词；如果完整 query 是 content 的子串就给固定基础分，否则按命中的查询词占比计算分数。  
没有任何词命中就不返回，结果再按分数和时间倒序排序，默认构造 prompt 时最多取 10 条。  
所以它成本低、可解释，但对同义词、隐含语义和复杂问法的召回能力有限。

### Q19：这套关键词检索怎样兼容中文和英文，`tokenize` 的真实行为是什么？

英文、数字及部分符号通过正则提取并转成小写集合，天然形成简单词袋。  
中文优先使用 jieba 分词；如果环境没有 jieba，就保留连续中文块并补充二元字符片段作为降级方案。  
检索匹配仍然是 token 是否出现在小写 content 中，不计算词频、BM25 或语义距离。  
因此中文可以工作，但分词质量和同义表达仍会明显影响召回。

### Q20：长期检索中的时间衰减具体怎么计算，24 小时之后是不是就完全检索不到了？

不是，只有走词袋匹配分支时才计算年龄，分数是命中比例乘以一个随小时下降的因子。  
这个因子从 1.0 线性下降，但最低为 0.5，所以到 24 小时及以后不会归零，只是不再继续降低。  
完整 query 子串命中走固定分数，不应用这段时间衰减。  
所以准确说法是“关键词匹配带 24 小时窗口内衰减和 0.5 下限”，不是严格的 TTL 或新鲜度淘汰。

### Q21：检索到长期记忆以后，系统怎样控制条数和 token 预算，为什么可能少于 10 条？

`build_context` 先按当前项目可见性和相关性取最多 10 条，再按顺序放入长期记忆上下文。  
每条使用 `MemoryEntry.token_count` 累加，如果下一条会超过动态 memory context budget，就直接停止。  
因此返回条数既受 top 10 限制，也受 token 预算限制，甚至可能因为第一条太大而一条都不注入。  
这里没有对单条事实做二次截断或跳过超大条目后继续尝试，这是可以演进的细节。

### Q22：为什么系统强调“按当前任务检索相关长期记忆”，而不是每次把全部长期记忆塞进 prompt？

全量注入会让无关事实占用上下文，增加跨项目污染、规则冲突和模型注意力稀释。  
当前实现用用户任务作为 query，只把可见且有关键词命中的长期事实按分数选入预算。  
这样长期记忆可以持续增长，而单次请求成本主要与相关结果数有关。  
不过由于检索不是语义检索，相关事实也可能漏召回，这个取舍要通过评测而不是口号判断。

### Q23：PAI.md 和长期记忆最终注入到模型的哪个位置，为什么选择 system prompt？

`prompt_context` 先拼接 PAI.md 项目记忆和“相关长期记忆”，调用方再把它们追加到基础 system prompt。  
chat 会更新 history 的第一条 system 消息，ReAct Agent 也会在每次任务开始时重建第一条 system 消息。  
放在 system prompt 能让项目规则和相关事实成为回答及工具决策的上层上下文。  
但内容来源仍可能不可信，尤其 PAI.md 是可编辑文本，所以不能把 system 层级误认为内容已经安全。

### Q24：上下文预算是怎样根据模型窗口动态计算的，为什么不能把阈值写死？

`ContextProfile` 读取 client 的 `max_context_window`，最低按 8K 处理，再动态计算短期预算、长期注入预算和压缩阈值。  
短期预算约为窗口的 45%，长期记忆注入预算是 `window / 200`，并限制在 500 到 5000 token。  
压缩阈值还会预留摘要空间和安全 buffer，避免历史顶满窗口后连摘要请求都发不出去。  
动态计算让同一套代码能适配不同上下文模型，但 token 本身仍是估算值。

### Q25：以 128K 上下文窗口为例，当前代码会得到哪些关键预算和阈值，请不要虚构压测结论？

按代码计算，128K 的短期 `ConversationMemory` 预算是 57,600 个估算 token，长期记忆注入预算是 640。  
history 压缩阈值会预留 20,000 摘要空间和 13,000 buffer，因此是 95,000 个估算 token。  
short_term 使用同一压缩比例作用于自身上限，触发点大约是 42,750 个估算 token。  
这些只是配置公式示例，不代表已经证明 128K 场景零溢出，仓库也没有可宣称的线上 P95 数据。

### Q26：为什么你反复强调 token 是估算值，这会给系统带来什么实际风险？

文本估算用字符比例，消息估算还会给每条消息加固定开销，并粗略估计图片与 tool call 参数。  
不同 provider 的 tokenizer、消息封装和图片计费规则都可能与这个模型不一致。  
低估时可能压缩过晚并触发上下文超限，高估时可能过早压缩导致信息损失。  
生产化时应接入 provider 精确 tokenizer 或使用实际 usage 做校准，同时保留安全余量。

### Q27：`ConversationMemory` 这一短期结构是怎样工作的，它会自动淘汰最老消息吗？

它用 `OrderedDict` 按 id 保存 `MemoryEntry`，并维护估算 token 总数。  
新增 user、assistant 和截断后的 tool result 都会写入，但 `store` 本身不会因为超过 max_tokens 自动淘汰旧条目。  
达到比例阈值后，`ContextCompressor` 会把全部 short_term 条目替换成一条 summary。  
所以它采用整体摘要而不是 LRU 淘汰，而且这份结构并不直接作为真实 LLM history 发送。

### Q28：为什么说 `history` 和 `ConversationMemory` 是两套并行结构，这是不是当前实现的技术债？

是的，真实发给 LLM 的是 `list[Message] history`，而 `ConversationMemory` 是另一份 MemoryEntry 形式的旁路短期记忆。  
用户和最终 assistant 内容会同时写两边，工具结果在 short_term 中会截断，但 history 中保留另一份原始 tool 消息。  
history 压缩和 short_term 压缩也分别调用不同 compressor，摘要结果不会互相同步。  
这会带来状态重复和语义漂移，是当前需要诚实说明并优先收敛的技术债。

### Q29：在交互式 chat 中，一次普通用户请求从进入系统到拿到回答，记忆链路完整经过哪些步骤？

系统先把用户输入写入 short_term，再实时读取 PAI.md，并按输入检索相关长期记忆。  
两类上下文被拼到 system prompt，用户消息同时追加到真实 history。  
随后分别检查 short_term 和 history 是否需要压缩，再携带 history 调用模型和工具。  
最终回答追加到 history 和 short_term；只有用户明确要求保存时，事实才进入长期 JSON。

### Q30：在 ReAct Agent 中记忆注入和历史压缩是怎样接入循环的，与普通 chat 有什么差异？

ReAct Agent 在 `run_json` 开始时用 task 检索记忆，并重建 history 第一条 system prompt，然后追加当前 task。  
每次 ReAct 迭代调用模型前都会检查真实 history 是否达到压缩阈值。  
工具调用及其原始输出进入 history，同时最多 500 字符的版本进入 short_term。  
它同样不会自动把整个任务抽取成长期事实，成功的最终回答只加入当前 short_term。

### Q31：工具结果为什么限制为 500 字符，这个截断实际覆盖哪些地方，又没有覆盖哪些地方？

`MemoryManager.add_tool_result` 最多保留前 500 个字符，超出后加“已截断”标记，目的是避免旁路 short_term 被大工具输出快速撑满。  
这个限制只作用于 `ConversationMemory` 中的 TOOL_RESULT 条目。  
chat 和 ReAct 的真实 history 会先追加完整的 `Message.tool`，并不会因为这个常量自动截断。  
因此“大工具输出已全面受控”是不准确的，真实 history 仍可能被工具结果推高并依赖后续压缩。

### Q32：真实会话 history 在什么条件下自动压缩，压缩时 system prompt 会不会被摘要掉？

每次调用前会用 `estimate_message_tokens` 估算 history，总量达到动态 `compression_trigger_tokens` 才触发。  
如果第一条是 system 消息，compactor 会把它排除在旧历史摘要之外并原样保留。  
它只压缩较早的非 system 消息，然后插入一对“已压缩摘要”的 user 和确认 assistant 消息。  
如果可压缩的 user 轮次不足，即使手动调用也会返回 false。

### Q33：自动压缩为什么默认保留最近 3 个 user rounds，代码如何确定一轮的边界？

compactor 会找出 system 之后所有 role 为 user 的消息索引，并以倒数第三个 user 作为切分点。  
切分点之前的旧消息进入摘要，切分点及其后的消息全部原样保留，所以工具消息和 assistant 消息会跟随所在近期区间保留。  
保留最近三轮能减少摘要损失对当前任务连续性的影响。  
这里的 round 是按 user 消息边界定义，不是严格验证完整 user-assistant 配对。

### Q34：用户执行 `/compact` 时与自动压缩有什么不同，为什么强制压缩只保留最近 1 轮？

自动压缩只有达到动态 token 阈值才执行，并默认保留最近 3 个 user rounds。  
`/compact` 直接调用 `compact_now`，不看 token 阈值，但仍要求历史中至少有超过 1 个 user round。  
强制模式只保留最近 1 轮，把更早内容尽量压成摘要，适合用户主动释放上下文。  
它不是清空历史；`/clear` 才会清空 history 和 short_term，同时保留长期记忆。

### Q35：历史摘要具体要求模型保留什么，摘要输入自身又有什么上限？

摘要 prompt 要求保留用户目标、已完成关键操作、已达成结论和未解决问题，并删除无关闲聊。  
旧消息会按角色展开，assistant 的 tool call 名称和参数也会附在 transcript 中。  
摘要输入最多 60,000 个字符，超过时停止继续拼接并标注超长内容已截断。  
输出被要求为一到三段中文，但这是提示约束，不是确定性的结构化校验。

### Q36：如果压缩模型超时、报错或返回空摘要，主流程会怎样，为什么这样设计？

对 `OSError` 和 `RuntimeError`，压缩器只记录 warning 并返回 false 或 None。  
返回空内容也视为压缩失败，不会改写原来的 history 或 short_term。  
调用方不会因此终止主任务，而是继续使用未压缩历史请求模型。  
这体现了“记忆增强失败不阻断主流程”，但如果原历史已经逼近硬窗口，后续模型请求仍可能因上下文过长失败。

### Q37：LLM 生成的历史摘要会不会遗漏事实或产生幻觉，你如何控制这个风险？

会，摘要是模型生成的，提示词要求不要添加原文不存在的事实，但没有确定性保证。  
当前缓解手段是保留最近几轮原文、保留 system prompt，并且只有拿到非空摘要后才原地替换历史。  
系统没有摘要事实校验、引用回链或可逆归档，所以较早细节可能永久从当前 history 消失。  
高风险场景应增加结构化摘要、关键事实校验，或者保留可追溯的原始会话存档。

### Q38：如果长期记忆被错误事实污染，当前系统有哪些发现和清理手段，又缺少哪些自动治理能力？

用户可以用 `/memory list` 查看全部记录，用 search 定位，再按 id delete，必要时 clear。  
project scope 能限制一部分污染范围，显式保存也减少了无意写入。  
但当前没有事实可信度、审核状态、来源证据、自动异常检测或回滚版本。  
所以污染治理主要依赖用户可见性和手工删除，不能把它描述成自愈知识库。

### Q39：如果同一主题先后保存了两个互相冲突的事实，检索和注入时系统会如何表现？

只要两条 content 不是完全相同，它们都会被保存，系统不会判断哪一条更新或更可信。  
检索时两条都可能因为关键词命中而进入候选，再按匹配分和 timestamp 排序。  
较新的时间只能用于同分排序，词袋分支还有时间衰减，但这不等于冲突消解。  
最终模型可能同时看到矛盾事实，因此后续需要实体键、版本关系和显式 supersede 机制。

### Q40：长期记忆没有 TTL 会造成什么问题，现有时间衰减能不能替代过期机制？

没有 TTL 意味着旧事实会永久保存在 JSON 中，除非用户主动删除或 clear。  
时间衰减只影响部分检索分数，而且 24 小时后仍保留 0.5 下限，完整子串命中甚至不衰减。  
因此过期的构建命令、版本号或用户偏好仍可能被召回。  
时间衰减不能替代 TTL，生产化应按事实类型增加有效期、最后确认时间和失效流程。

### Q41：从隐私和安全角度看，这套记忆系统当前有哪些明确边界？

长期记忆默认是本机明文 JSON，PAI.md 也是普通文本文件，没有加密、脱敏、权限系统或审计日志。  
global 事实还可能跨项目暴露，错误 scope 会放大敏感信息传播范围。  
路径沙盒只约束 PAI import 不能逃出来源根目录，并不检测文件内容里是否包含提示注入或秘密。  
所以当前适合保存低敏感项目规则和偏好，不适合直接承载凭据、个人隐私和受监管数据。

### Q42：如果仓库里的 PAI.md 被恶意修改并写入提示注入指令，现有 import 沙盒能防住吗？

防不住，import 沙盒解决的是“能读取哪个路径”，不是“读取到的内容是否可信”。  
PAI.md 内容会进入 system prompt，恶意规则可能影响模型回答和工具决策。  
当前 chat 工具本身限制为读文件、搜代码和显式保存记忆，能缩小后果，但不是完整防护。  
生产环境应结合仓库信任策略、规则签名或审批、敏感指令过滤和工具侧确定性授权。

### Q43：同一进程里开多个会话，或者一个会话读、另一个会话写长期记忆时，能保证立即一致吗？

如果会话共享同一个 `LongTermMemory` 实例，线程内写操作由 RLock 串行，后续检索能看到更新后的内存字典。  
如果各自创建实例，它们只在初始化时加载文件，不会自动监听其他实例的写入。  
即使在同一进程，不共享实例也可能长期持有旧快照。  
当前没有文件变更监听、刷新协议或版本号，因此一致性只能算单实例内的基础一致性。

### Q44：损坏 JSON 被忽略虽然保证了可用性，会不会造成静默数据丢失，你会怎样改进可观测性？

会，当前加载失败只写 warning，然后以空记忆继续，用户如果没看日志可能以为原本就没有记忆。  
而且后续一次成功保存可能用当前空快照覆盖损坏文件，进一步降低恢复机会。  
我会增加显式健康状态、备份损坏文件、CLI status 告警和写入前恢复检查。  
更进一步可以加校验和、滚动备份和恢复命令，但当前代码尚未实现这些能力。

### Q45：用户目前可以通过哪些命令管理记忆，这些命令分别改变哪一层？

`/save [--global] FACT` 写长期 JSON，`/memory` 支持 status、list、search、delete 和 clear。  
`/init [--force]` 创建或覆盖项目根目录的 PAI.md 模板，不带 force 时会保留现有文件。  
`/compact` 只压缩真实会话 history，`/clear` 清空 history 和 short_term，但保留长期记忆。  
这些边界让用户能区分“清当前会话”和“删长期事实”，减少误操作。

### Q46：如果面试官追问这套记忆系统上线后的核心指标，你会给出哪些指标，当前有没有真实数据？

当前仓库没有大规模线上统计，所以我不会编造命中率、节省比例或 P95。  
效果侧我会看长期记忆 Recall@K、Precision@K、PAI 规则遵循率、摘要关键事实保留率和冲突事实触发率。  
成本侧看每请求注入 token、压缩频率、摘要 token 成本、上下文超限率和端到端延迟。  
可靠性侧还要看 JSON 写入失败、损坏恢复、跨进程丢写、压缩失败和人工删除率。

### Q47：你会怎样离线评测关键词检索、时间衰减和按预算注入，避免只靠主观体验？

我会构造带 project/global scope、同义改写、冲突事实、新旧事实和超长事实的标注查询集。  
对每个查询记录期望相关条目，计算 Recall@1、Recall@10、Precision@10、MRR，并单独分析中文分词和跨项目污染。  
再用不同 context window 验证 token 预算、排序和截断行为，比较词袋基线与未来 embedding 或混合检索。  
时间衰减还要用固定时钟测试 0、12、24 和 48 小时边界，当前测试集尚未覆盖这些完整指标。

### Q48：压力题：如果 PAI.md 接近 24,000 字符、真实 tool history 很大、token 又被低估，同时压缩服务失败，系统还能保证请求成功吗？

不能保证，PAI.md 会先按字符预算截断，但真实 tool 消息没有 500 字符限制，token 估算也可能低于 provider 实际值。  
压缩失败时系统为了不阻断业务会保留原 history，下一次主模型调用可能直接触发上下文超限。  
当前优势是失败不会破坏历史，缺点是缺少调用前的 provider 精确硬预算和确定性降级截断。  
上线前应增加精确计数、工具结果分层存储、超限前硬裁剪，以及摘要服务失败时的本地确定性压缩策略。

### Q49：如果让你把这套记忆能力逐步上线，你会如何安排演进顺序？

第一阶段我会保留显式保存和关键词检索，先补监控、备份、敏感信息提示、跨进程锁和写入失败反馈。  
第二阶段统一 history 与 ConversationMemory，给工具结果做引用式存储，并加入 TTL、冲突版本和事实来源。  
第三阶段在有离线标注集后引入 embedding 加关键词的混合检索，同时保留 project/global 过滤和可解释分数。  
最后才考虑自动事实抽取，而且必须经过用户确认、隐私策略和可撤销审计，不能为了“智能”牺牲控制权。

### Q50：请你用一段完整的架构师口径总结这套三层记忆方案最值得讲的亮点、当前边界和下一步方向？

这套方案的亮点是把稳定项目规则、跨会话事实和当前会话历史分层管理：PAI.md 每次实时读取，长期事实按任务相关性和 scope 检索，早期 history 在动态预算下压缩并保留近期轮次。  
工程上还有 PAI import 路径沙盒、JSON 临时文件 replace、单进程 RLock，以及压缩失败不阻断主流程这些务实保护。  
但我会明确说明它目前只是关键词词袋检索，没有向量库；长期 JSON 明文且显式写入，也没有 TTL、自动冲突合并、可信度、版本冲突、加密或权限系统。  
另外 history 与 short_term 尚未统一、真实 tool history 不自动截断、多进程仍可能 last-write-wins，这些都是上线前应按可靠性优先补齐的技术债。

## 10. 亮点 6 口语逐字稿（只读工具并行、写操作串行：Q1-Q46）

### Q1：请你先完整介绍一下“只读工具并行、写操作串行”这个设计解决了什么问题，以及它最核心的价值是什么？

ReAct 一轮里经常要同时读目标文件、搜调用方和查看测试，这些独立读取如果逐个等待，会把工具阶段延迟直接累加。  
我的策略是只有同一轮存在多个调用，而且 runtime 判定它们全部只读时，才用 `asyncio.gather` 并行执行。  
只要其中出现任何写操作或有副作用的命令，整批就严格按模型给出的原顺序串行。  
它的核心价值是在不放松写安全边界的前提下，缩短独立证据收集的 wall-clock 时间。

### Q2：为什么你选择并行工具调用，而不是只优化单个工具本身的执行速度？

单工具优化当然有价值，但文件读取、代码搜索和上下文收集往往彼此独立，串行等待本身就是结构性浪费。  
如果三个工具各耗时一秒，串行工具阶段接近三秒，并行后的理想下界更接近最慢的那一个。  
这种优化不要求改写每个业务工具，也不会把并发逻辑散落到搜索器、读取器和验证器内部。  
所以我先消除调用链上的可并行等待，再分别考虑单工具性能。

### Q3：从 LLM 返回响应到工具结果重新进入上下文，完整调用链是怎样的？

`ReactAgent.run_json` 先调用 LLM，读取响应里的 `tool_calls`，并把带调用列表的 assistant 消息写入 history。  
随后它通过 `_run_async` 驱动 `_execute_tool_calls`，由后者判断这一批是并行还是串行，再由 `_execute_one` 执行每个工具。  
执行结束后得到 `ToolTrace` 列表，runtime 按原调用列表与 trace 列表 `zip`，用对应的 tool-call id 追加 tool 消息。  
模型下一轮因此能看到每个调用的正确结果，并继续推理或输出最终 JSON。

### Q4：一次 LLM 响应为什么能够包含多个 tool calls，系统又是在哪里识别这一点的？

OpenAI-compatible 的响应协议允许 assistant 在同一条响应中返回一个 `tool_calls` 列表，而不是每轮只能调用一个函数。  
代码通过 `response.has_tool_calls()` 和 `response.tool_calls or []` 取得这一整批调用。  
只有列表长度大于一，才存在批内并行的意义；单个调用仍走同一个执行入口但不会进入并行分支。  
提示词也会提醒模型把相互独立的证据请求放在同一 assistant turn。

### Q5：`is_read_only` 到底是根据工具名称硬编码，还是由调度器自己分析参数和副作用？

它是各个 `ReactToolRuntime` 提供的运行时声明或分类，统一调度器只调用 `tools.is_read_only(call.name)`。  
调度器不会静态分析 Python 函数，也不会根据参数推断它是否真的无副作用。  
这种设计让 Modifier、Verifier 和 Test Generator 能按自己的业务语义定义边界。  
代价是分类必须可信，误把写工具标成只读就可能引入竞态。

### Q6：并行分支的精确触发条件是什么，为什么要同时判断“多个调用”和“全部只读”？

条件就是 `len(calls) > 1`，并且对所有调用执行 `all(tools.is_read_only(call.name))` 都为真。  
多个调用保证并行有实际意义，全部只读则保证这一批没有已知的外部写副作用。  
只满足其中一个条件都走串行路径，空列表直接返回空结果。  
这是保守的全有或全无策略，不会对混合批次做局部并行。

### Q7：为什么一批工具里只要混入一个写操作，就连其他只读工具也全部串行？

因为模型给出的调用顺序可能隐含“先读、再写、再读”的状态依赖，而当前 runtime 没有显式依赖图。  
如果擅自把只读子集提前并行，后面的读取可能看到写前状态，或者本应先完成的读取与写入发生重叠。  
所以代码选择整批按 LLM `tool_calls` 原顺序执行，用性能换确定性。  
当前没有“先并行读、再串行写”的 DAG 优化，我会把它明确说成边界而不是能力。

### Q8：`asyncio.gather` 在这里具体承担什么职责，实际的同步工具是怎样并发起来的？

`asyncio.gather` 同时调度这一批 `_execute_one` 协程，并等待它们全部产生结果。  
但业务 runtime 的 `execute` 是同步函数，所以 `_execute_one` 再用 `asyncio.to_thread` 把阻塞调用放到线程池。  
也就是说，AsyncIO 负责协程编排，真正的文件搜索或同步命令在线程中运行。  
这样既保留现有同步工具接口，又能并发执行独立只读任务。

### Q9：既然工具本身是同步函数，为什么不要求所有工具都改造成原生 async？

现有工具大量使用同步文件 API、AST 收集器和命令执行器，全面 async 化改造成本高，而且很多底层库本身也不是异步接口。  
`asyncio.to_thread` 提供了清晰的同步到异步桥接层，让统一 ReAct runtime 获得并发能力。  
它适合当前以阻塞 I/O 为主的工具，但对纯 CPU 密集任务仍受线程调度和 GIL 等因素限制。  
如果未来工具原生支持 async，可以再扩展 runtime 协议，而不是现在强制重写全部业务实现。

### Q10：并行执行后工具完成顺序不确定，系统如何保证返回结果仍与原来的 tool-call id 正确对应？

`asyncio.gather` 返回值的顺序与传入 awaitable 的顺序一致，不按实际完成先后排列。  
代码构造协程列表时沿用原始 `calls` 顺序，因此 `round_traces` 也保持这个顺序。  
后续用 `zip(calls, round_traces, strict=True)` 把每个 trace.output 写回对应的 `call.id`。  
要注意日志中的实际完成时间可能交错，但结果列表和 tool 消息顺序仍然稳定。

### Q11：每次工具执行记录了哪些 trace 信息，这些信息能支持什么排查和评测？

每个 `ToolTrace` 记录 `iteration`、工具 `name`、解析后的 `arguments`、`output` 和 `duration_ms`。  
这能回答某个 ReAct 轮次调用了什么、输入输出是什么，以及单工具耗时是多少。  
并行批次还会记录整批完成耗时，适合对比串行耗时之和与并行 wall-clock。  
不过当前 trace 不是完整分布式链路追踪，也没有线程 id、排队时间或统一 trace id。

### Q12：为什么把并行策略放在统一 ReAct runtime，而不是让每个业务工具自己决定是否并行？

工具本身最清楚副作用分类，但批量调度、结果排序、异常传播和 trace 记录属于通用执行语义。  
把策略放在 `react.py` 的 `_execute_tool_calls`，所有 Agent 都能复用同一套规则。  
业务 runtime 只需实现 schema、execute 和 `is_read_only`，避免每个工具各写一套并发代码。  
这也让安全策略集中可审计，而不是散落在 Modifier、Verifier 和 Test Generator 里。

### Q13：如果两个被标记为“只读”的工具内部都会更新缓存，这还算安全吗？

要区分外部业务副作用和 runtime 内部可变状态；当前 `is_read_only` 主要表达前者。  
如果两个工具会无锁修改同一缓存、懒加载字段或统计计数，即使不写仓库，也可能发生数据竞态。  
当前共享 runtime 没有通用锁或隔离机制，所以误标或线程不安全的内部缓存确实是风险。  
生产化时我会要求只读工具线程安全，或把缓存做成不可变快照、加锁，必要时直接分类为非只读。

### Q14：`asyncio.gather` 中一个工具抛出未捕获异常时会发生什么，本轮 Agent 会怎样结束？

当前 `gather` 使用默认的 `return_exceptions=False`，首个未捕获异常会向上传播，使本批工具执行失败。  
`run_json` 捕获后会返回 `Tool execution failed`，因此这次 Agent 运行终止，而不是把异常当作普通工具结果继续推理。  
多数业务 `execute` 会把可预期的参数、补丁或文件错误转成 JSON，这类错误不会抛出。  
但未捕获异常仍会终止 Agent，当前调度层没有自动重试或按工具降级。

### Q15：一个并行工具失败以后，其他已经提交到 `to_thread` 的工具会被真正取消吗？

不能保证，协程层收到异常或取消，不等于已经启动的底层线程能被强制停止。  
`asyncio.to_thread` 使用的同步函数一旦在线程里运行，Python 没有安全的通用机制把它直接杀掉。  
因此本轮可能已经返回失败，但其他线程仍在后台继续读文件或执行底层逻辑。  
这也是为什么只有无副作用工具才允许并行；如果误标写工具，失败后的残留线程会放大风险。

### Q16：当前有没有 per-tool timeout，超时以后取消语义是什么？

统一 scheduler 本身没有给 `_execute_one` 包 `asyncio.wait_for` 或 `asyncio.timeout`，所以没有 per-tool timeout。  
部分底层命令执行器有自己的 timeout，但那是业务命令层能力，不能等同于统一工具超时。  
即使未来给 `to_thread` 外层加超时，也只能停止等待，未必能终止已经运行的同步线程。  
真正可控的硬超时需要工具配合取消令牌，或者把高风险任务放进可终止的子进程。

### Q17：共享同一个 runtime 实例并行执行多个工具，会有哪些竞态风险？

只读 toolbox 主要读取仓库，风险较小，但不同 Agent runtime 还可能维护 application、coverage、diff 缓存和版本字段。  
当前安全性依赖 `is_read_only` 把会修改关键状态的调用挡在并行分支之外，而不是 runtime 天生不可变。  
像缓存型“读取”如果并发写内部字段，也可能重复计算或产生竞态，代码没有统一线程安全保证。  
因此分类审计和共享状态治理是这套方案成立的前提。

### Q18：写操作为什么必须串行，除了文件覆盖之外还有哪些架构层面的原因？

写操作会改变后续工具看到的仓库快照、runtime 版本和验证证据，执行顺序本身就是业务语义。  
并发写不仅可能覆盖同一文件，还可能让 patch 快照、JaCoCo 产物、Maven target 和缓存失去一致性。  
串行执行能让每一步基于前一步完成后的确定状态，也方便按 trace 复盘。  
它不能替代事务、幂等和回滚，但先消除了最直接的并发写竞态。

### Q19：Modifier Agent 的工具是怎样分类的，哪些可以并行，哪些必须串行？

`ModifierToolRuntime` 复用 toolbox 的 `get_issue_context`、`read_file`、`search_code`、`get_plan_context` 和 `get_verification_feedback`，这些按只读分类。  
`apply_edits` 明确返回非只读，因为它会生成变更、写文件、保存快照并更新 `application` 状态。  
所以同轮多个独立证据读取可以并行，但只要与 `apply_edits` 同批出现，整批按模型原顺序串行。  
另外一次 attempt 中成功应用后会阻止再次成功应用，这是一层业务保护。

### Q20：Verifier Agent 明明不修改生产源码，为什么 `run_verification_command` 仍然被视为非只读？

验证命令可能生成或修改 `target`、JaCoCo 报告、测试结果和其他构建状态，所以不能按“没有改源码”就视为只读。  
`VerifierToolRuntime.is_read_only` 专门把 `run_verification_command` 分类为非只读。  
这样多个 Maven 命令不会在同一批里并发争抢构建目录，混合的 diff 或 coverage 读取也不会越过命令顺序。  
这是按可观察副作用而不是按工具名称表面含义做分类。

### Q21：Verifier 里的 `inspect_diff` 和 `get_coverage_assessment` 为什么可以进入只读并行分支，它们完全没有状态变化吗？

从仓库业务语义看，它们读取 diff 和覆盖率证据，不主动运行构建命令，所以被声明为只读。  
但实现内部会更新 `diff_inspected`、`coverage_assessed` 和缓存字段，因此并非严格的纯函数。  
当前假设这些缓存更新彼此可接受且命令调用被串行隔离，但没有通用线程安全证明。  
生产化时应对这类内部状态加锁、去重或改成返回不可变结果，不能只看外部文件是否被写。

### Q22：Test Generator Agent 的工具分类是什么，为什么预检也不能并行？

它的上下文读取和 `inspect_test_conventions` 被视为只读，可以在同轮独立调用时并行。  
`apply_test_edits` 会创建或修订测试文件，所以是写操作。  
`run_generated_test_precheck` 会执行编译、测试和 JaCoCo，产生构建状态并更新版本、命令和 coverage 字段，也属于有副作用操作。  
因此这两个工具都不能进入只读并行分支，而且预检在业务上还依赖前一次测试写入成功。

### Q23：同步的 `run_json` 为什么需要 `_run_async`，这层桥接具体怎么工作？

Agent 对外暴露的是同步调用，但 LLM client、记忆压缩和批量工具调度是协程，所以需要同步到异步的桥。  
`_run_async` 先检查当前线程是否已有 active event loop，没有就交给 `_run_on_sync_loop`。  
后者懒加载模块级 `_SYNC_LOOP`，并用 `run_until_complete` 驱动协程，后续同步调用会复用这个 loop。  
现有测试也验证了连续调用复用同一个 event loop。

### Q24：如果在 FastAPI、Jupyter 或其他已经运行 event loop 的环境里调用 `_run_async`，会发生什么？

它不会嵌套执行，而是先关闭尚未 await 的 coroutine，然后明确抛出 `RuntimeError`。  
错误信息是同步 refactor-agent LLM 调用不能运行在 active asyncio event loop 内。  
因此当前同步 API 不能直接嵌入异步请求处理函数，这不是自动兼容的。  
正确演进方向是提供原生 async 入口，由上层 `await`，而不是用嵌套 loop 或补丁绕过限制。

### Q25：模块级 `_SYNC_LOOP` 在多线程、多会话并发场景下有什么问题？

它是进程内共享的模块级可变对象，没有线程局部隔离，也没有围绕创建和 `run_until_complete` 的并发锁。  
多个线程或会话同时调用时，可能争用同一个 event loop，而 event loop 也不适合被不同线程并发驱动。  
所以当前实现适合受控的同步单调用路径，不应宣称已经支持多会话高并发。  
生产服务应采用每请求原生 async、专用 loop 线程，或者明确的线程隔离和生命周期管理。

### Q26：当前有没有背压、Semaphore 或每批并发上限，如果模型一次返回很多只读调用会怎样？

当前没有 semaphore、队列容量或 per-tool 并发上限，`gather` 会为这一批所有只读调用创建 awaitable。  
`to_thread` 最终受默认线程池 worker 数限制，超出的任务会排队，但这不是业务可控的背压策略。  
调用过多时仍可能造成内存、文件句柄、磁盘和线程池排队压力。  
所以这套实现解决了基础并行，不等于已经完成生产级流量治理。

### Q27：如果线程池耗尽，会出现什么现象，你会如何定位？

新的 `to_thread` 任务会在线程池队列中等待，表现为工具 wall-clock 上升，而不是无限创建线程后立即执行。  
其他共用默认 executor 的异步任务也可能被拖慢，形成跨功能资源争抢。  
当前 trace 只有总 duration，排队时间和执行时间没有拆开，因此定位能力有限。  
我会增加 executor 队列、活跃 worker、等待时长和工具类型指标，并考虑给该 runtime 配独立受限线程池。

### Q28：并行读取会不会造成磁盘、CPU、网络或外部服务资源争抢，为什么不是并发越高越好？

会，多个代码搜索可能同时遍历同一仓库，既争抢磁盘缓存，也可能重复消耗 CPU 和内存。  
如果工具访问远端服务，还可能触发连接池上限或对方限流。  
并行收益只在独立等待占主导且资源尚未饱和时成立，超过拐点后吞吐和尾延迟都可能恶化。  
所以生产参数要通过压测确定，而不是把调用数量直接等同于线程数量。

### Q29：工具幂等性与这套并行策略有什么关系，当前代码是否保证所有只读工具都幂等？

只读和幂等相关但不等价；一个工具不写仓库，也可能读取时钟、变化中的文件或更新内部缓存，重复结果未必完全一致。  
当前代码主要依靠 runtime 分类副作用，没有统一的幂等性契约或自动验证。  
并行分支最好只接纳线程安全、可重复读取、失败可重试的工具。  
写工具即使设计成幂等，当前仍不会并行，因为状态顺序和共享资源风险依然存在。

### Q30：如果第二个工具依赖第一个工具的输出，模型应该怎样调用，runtime 能自动识别依赖吗？

当前 runtime 不解析参数之间的数据依赖，也不构建工具调用 DAG。  
依赖调用必须由模型分成不同 ReAct 轮次：先调用第一个工具，把结果写回 history，再在下一 assistant turn 生成第二个调用。  
`prompts.py` 也只鼓励把“相互独立”的工具放在同一轮，不鼓励强行合并依赖步骤。  
如果模型把依赖调用错误地放进同一全只读批次，scheduler 不会替它纠正。

### Q31：并行是否会跨越多个 ReAct 轮次，或者让不同 Agent 的工具同时执行？

不会，当前并行只发生在同一个 assistant turn 返回的多个 tool call 之间。  
每批结果写回 history 后，Agent 才发起下一轮 LLM 请求，因此跨轮仍是顺序推进。  
Modifier、Verifier 和 Test Generator 的工作流调度也不是由这里跨 Agent 并行化的。  
所以这个亮点应准确表述为“同轮独立只读工具并行”，不是整个重构流水线并行。

### Q32：为什么这里使用 `asyncio.gather` 而不是 Python 3.11 的 `TaskGroup`？

当前需求是提交一组独立协程，并按输入位置直接拿回同序结果，`gather` 的返回语义很贴合。  
`TaskGroup` 提供更强的结构化并发和兄弟任务取消语义，但需要自己保存 task 引用并重建有序结果，还会引入 `ExceptionGroup` 处理。  
代码里没有记录一份正式的选型 ADR，所以我不会声称经过复杂基准后证明 gather 更优。  
如果未来重点转向严格的失败收敛和分层取消，TaskGroup 是值得评估的演进方案。

### Q33：并行工具的日志顺序和结果顺序是否一致，排查问题时应该相信哪一个？

结果顺序由 `gather` 保证与输入顺序一致，后续 tool-call id 映射应以结果列表为准。  
各线程实际完成时间不确定，所以如果工具内部各自打日志，日志可能按完成先后交错。  
批次日志只说明开始并行和整批完成耗时，不代表逐工具按列表顺序完成。  
排查时应结合 iteration、工具名、arguments 和 trace，而不能从日志行先后推断依赖关系。

### Q34：现有自动化测试具体证明了什么，又有哪些关键行为还没有被充分覆盖？

`test_llm_executes_multiple_tool_calls_in_parallel` 用两个只读调用、线程 Barrier 和线程 id 集合证明它们确实在不同 worker 中并发。  
测试还断言 tool 消息 id 仍按 `tool-read`、`tool-search` 的原顺序，并检查并行开始与完成日志。  
同步桥接测试证明连续 `_run_async` 调用复用模块级 event loop。  
但还应补混合读写整批串行、异常传播、误分类竞态、超时和大批量背压测试，现有测试不能证明这些生产性质。

### Q35：你说工具阶段平均耗时降低 57.6%，这个 benchmark 的公平比较口径是什么？

我只会按项目内 benchmark 口径表述：选择同批相互独立的只读工具组合，对比串行执行耗时之和与并行执行的 wall-clock。  
两组必须保持工具名称、参数、代码版本、机器负载、cache 环境和预热策略一致，并做多次重复后报告统计量。  
仓库有单工具 `duration_ms` 和批次计时，但没有可复算 57.6% 的大规模原始数据文件。  
因此我不会虚构样本数、原始秒数或统计显著性，只说这是项目内评测结果。

### Q36：57.6% 是否包含 LLM 推理和网络请求时间，为什么必须把统计范围讲清楚？

这个数字按当前项目口径指工具阶段，不包含前后的 LLM 推理与 API 网络时间。  
如果把 LLM 时间混进去，并行工具节省会被其他固定成本稀释，而且不同响应波动会破坏可比性。  
端到端延迟可以另外测，但必须单独报告并固定模型、输入和缓存条件。  
所以我会明确区分 tool-stage wall-clock 与完整 Agent wall-clock。

### Q37：请你用 Amdahl 定律解释为什么工具阶段降低 57.6%，端到端延迟不一定降低同样比例。

Amdahl 定律说明总体加速受可并行部分占比限制，串行部分再小也会成为上限。  
这里只并行同轮的独立只读工具，LLM 调用、跨轮推理、写操作和验证命令仍然串行。  
并行批次本身还受最慢工具决定，并有线程调度和资源争抢开销。  
所以 57.6% 不能直接外推成端到端也降低 57.6%，实际收益取决于工具阶段在总耗时中的占比。

### Q38：除了平均耗时，你会用哪些指标判断这套并行策略是否真的可上线？

性能侧我会看批次 wall-clock、串行耗时之和、加速比、P50、P95、P99 和线程池排队时间。  
可靠性侧看工具异常率、超时率、取消后残留任务、结果错配率和写读竞态事件。  
资源侧看活跃线程、队列深度、CPU、内存、文件句柄、磁盘吞吐和外部服务限流。  
还要按工具类型和批大小分桶，否则一个平均数会掩盖尾延迟与资源饱和。

### Q39：并行执行是否会增加 Token 消耗，为什么？

在工具集合、参数和输出完全相同的前提下，仅把执行方式从串行改成并行，不会天然增加工具输出 Token。  
它主要减少等待时间，tool 消息仍按原调用逐条回填。  
但提示模型同轮发更多调用，可能改变模型规划、调用数量或返回内容，因此端到端 Token 仍需实测。  
所以我不会把“并行”直接宣传成 Token 优化，它首先是延迟优化。

### Q40：如果模型每轮始终只发一个 tool call，这套并行机制还能带来收益吗？

不能，因为触发条件明确要求同一轮调用数量大于一。  
系统 prompt 会提示把独立证据调用放在同一 assistant turn，但最终是否这样规划仍由模型决定。  
可以通过模型能力评测、few-shot 示例和调用轨迹指标提高批量调用率，但不能由 scheduler 凭空合并跨轮请求。  
如果强制自动合并，还必须先解决语义依赖识别问题。

### Q41：压力题：同一批里一个写工具夹在两个读工具中间，为什么不先并行两个读，再执行写？

因为第二个读在模型顺序上位于写之后，它可能就是要观察写后的状态，不能与第一个读等价处理。  
当前调用协议没有显式声明每个节点依赖谁，调度器无法安全证明把它提前是等价的。  
所以只要批中有一个非只读工具，三个调用全部按模型原顺序串行。  
未来只有在引入显式依赖 DAG、状态版本和冲突检测后，才适合做分层调度。

### Q42：压力题：如果线上出现慢工具、线程池耗尽和连续失败，你会怎样产品化加入并发上限、timeout 与 circuit breaker？

我会先按工具类型设置 semaphore 和独立 executor，限制单批及全局并发，并用有界队列形成背压。  
每个工具增加软超时、整体 deadline 和取消令牌；无法协作取消的命令放到子进程，超时后终止进程树。  
对外部依赖按错误率和延迟做 circuit breaker，配合指数退避、抖动和只对幂等读工具重试。  
这些能力当前都没有实现，也没有优先级、限流、跨进程调度或分布式队列。

### Q43：压力题：并发执行时如何处理重试，为什么不能对所有失败工具统一自动重试？

只读且幂等的瞬时失败工具可以在受限次数内重试，但要计入整体 deadline，并防止重试风暴。  
写工具、验证命令和状态型工具不能默认重试，因为第一次可能已经部分成功，第二次会重复副作用。  
当前 scheduler 没有内建重试，业务工具多数只是把可预期错误转成 JSON 交给模型处理。  
产品化时应把 retry policy 作为工具元数据，而不是只按异常类型一刀切。

### Q44：压力题：这套方案有没有跨进程调度、任务优先级、熔断、限流或分布式队列能力？

都没有，当前实现是单进程内、单 ReAct 批次的 AsyncIO 加线程池调度。  
它没有跨进程 worker、优先级队列、统一熔断器、全局速率限制或持久化任务队列。  
模块级 `_SYNC_LOOP` 也不适合作为多线程多会话的调度中心。  
所以我会把它定位成清晰有效的本地并发机制，而不是分布式任务平台。

### Q45：如果让你把当前实现演进到生产级，你会按什么顺序推进，为什么？

第一步先补混合批串行、异常、超时和压力测试，同时完善批次、队列和资源指标，确认现状可观测。  
第二步加入工具能力元数据、全局与分工具 semaphore、独立线程池、deadline 和只读幂等重试。  
第三步把同步 API 改造成原生 async，并治理 runtime 共享状态、缓存线程安全和可协作取消。  
最后才考虑依赖 DAG、读写分层调度、子进程隔离和分布式队列，因为这些复杂度必须由真实规模需求驱动。

### Q46：请你用一段架构师口径总结这个亮点的设计取舍、真实收益和明确边界。

我的核心设计是把并行策略集中在统一 ReAct runtime：只有同一 assistant turn 返回多个调用且 runtime 判定全部只读时才 `gather`，任何写或副作用调用存在就整批按模型原顺序串行。  
同步工具通过 `to_thread` 执行，`gather` 保持输入结果顺序，再用 tool-call id 正确回填；每个调用都有 iteration、name、arguments、output 和 duration trace。  
57.6% 只按项目内独立只读工具 benchmark 的工具阶段口径讲，理论收益受最慢工具、LLM 时间和串行写阶段限制。  
边界也很清楚：分类可能误标，线程未必可取消，当前没有统一 timeout、并发上限、背压、重试、熔断、依赖图或分布式调度，active event loop 和模块级 `_SYNC_LOOP` 也限制了高并发服务化。